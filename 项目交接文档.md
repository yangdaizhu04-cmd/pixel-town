# 「拾光小镇」像素风生活工作台 · 项目交接文档

> 用途：粘贴到新会话/新规划中，让零上下文的协作者能无缝接手。
> 生成日期：2026-09-06 · 项目路径：`C:\Users\echo\Desktop\diy\灵感开发项目一\pixel-town`

---

## 一、最终目的

做一个**像素风、轻松治愈的游戏化生活工作台**（Web 单页应用）：把日常管理装进一座「小镇」的隐喻里，包含待办、记账、习惯打卡、AI 新闻、学习计划、英语练习、体重记录、每日复盘共 8 个生活模块 + 1 个可对话的 AI 智能体。核心爽点是**游戏化反馈闭环**：完成任何一件小事都获得 XP 和金币，并自动给花园里的植物浇水，让每天的小进步被看见。视觉参考用户提供的一张 bilibili 截图（「日常小镇」项目：奶油底+绿草+像素描边+左侧导航+礼箱进度），但明确要求**不照抄**，要原创衍生并参考网络上优秀的治愈系/游戏化设计汲取灵感。

## 二、关键背景

- **环境**：Windows 11 (win32 10.0.26200) + Git Bash；工作区 `C:\Users\echo\Desktop\diy\灵感开发项目一`；Node v24.16.0 / npm 11.13.0；非 git 仓库。当前日期以 2026-09 为准。
- **参考图要点**（用户提供的截图，非本项目代码）：顶部标题「欢迎来到日常小镇」、左侧图标导航、日期+问候横幅、绿色天气 banner、统计卡行、「今日冒险进度」XP 条+四档礼箱、「我的成长植物园」、底部「村长的提醒」。
- **设计调研结论**（已落地为设计原则）：Finch 的「宽恕优先」（永不惩罚断签）、Habitica 的 RPG 数值但要更轻量、治愈系 = 柔和粉彩 + 圆润块面 + 情感锚点（植物替代宠物）。
- **用户通过提问确认的三个关键选择**：① 交付形态 = Vite + React 工程（不是免安装单文件静态页）；② 智能体 = 接真实大模型（OpenAI 兼容接口），未配 Key 时本地规则兜底；③ 新闻 = 在线抓取 + 内置精选兜底。
- 用户明确说设计可使用 GSAP 相关 skill（已加载 gsap-core / gsap-react，遵循其 useGSAP/contextSafe/matchMedia 规范）。

## 三、已经确定的事实（实测验证过，勿再踩）

1. **字体**：`@fontsource/fusion-pixel-12px-proportional-sc` 只含 latin 子集，中文会回退。实际采用 `@vp-tw/cjk-web-fonts-fusion-pixel-font`（npm，分片 woff2），CSS 路径 `dist/12px/proportional/zh_hans/Fusion-Pixel-12px-Proportional-Simplified-Chinese.css`，font-family 名为 **`Fusion Pixel 12px Proportional Simplified Chinese`**。
2. **新闻源存活名单**（rss2json 实测）：量子位 `https://www.qbitai.com/feed` ✓、爱范儿 `https://www.ifanr.com/feed` ✓、InfoQ `https://www.infoq.cn/feed` ✓、Hacker News `https://hnrss.org/frontpage` ✓。机器之心与 36氪 的 RSS **已死**。
3. **rss2json 免费接口**（`api.rss2json.com/v1/api.json?rss_url=...`）**不能带 `count` 参数**（422 要求 API Key）；默认返回约 10 条。
4. **GSAP**：3.15.0 + @gsap/react 2.1.2 已装。`gsap.from()` 在 React StrictMode/HMR 双跑下连续调用会被污染（终点读到被 kill 的中间值，元素钉死在起点，如 canvas scaleY 卡 0.001）。**解法已全部落地：useEffect 里的入场动画一律 `gsap.fromTo` + `overwrite: true`；只有 useGSAP context 内可用 from。**
5. **file:// 交付**：`<script type="module">` 在 file:// 下必被 CORS 拦（与 base 路径无关）。已用 `vite-plugin-singlefile` 把 JS/CSS/字体全部内联为单个 `dist/index.html`（**2026-09-07 起约 0.6MB**，字体经 `scripts/subset_fonts.py` 子集化后仅内联项目用到的字符，见踩坑 25），Edge/Chrome 实测双击可用。
6. **localStorage**：存档 key `pixel-town-save-v1`（250ms 防抖自动保存）；新闻缓存 `pixel-town-news-v1`（6 小时）。file:// 与 http://localhost 属不同 origin，数据互相独立。
7. **Console**：最终检查无任何运行时错误（仅有已修复前的历史 RSS 422/500 残留）。
8. `Math.random()` 仅用于 toast id、彩带角度、随机文案——安全扫描（Mimosa）报"弱随机数"为**误报**，不处理。
9. dev 服务器默认端口 5173；build 产物 base='./' + singlefile。
10. **PWA 资产在 public/**：`sw.js / manifest.webmanifest / icon.svg` 随构建输出到 dist 根目录（不被 singlefile 内联），**仅线上部署时需要**；file:// 双击用法不依赖它们（SW 仅在 http(s) 注册）。`beforeinstallprompt` 只在满足安装条件时触发一次，App 层转成设置里的手动安装按钮。
11. **本地 HTTPS（2026-09-07 已配通）**：`scripts/https-setup.ps1` 用 mkcert（v1.4.4，装于 `~\.local\bin`）一键生成 `.https/cert.pem + key.pem`（SAN 覆盖 localhost + 全部局域网 IP），vite.config.js 检测到 `.https/` 即自动启用 `server.https`；mkcert 下载走 ghproxy.net 镜像（GitHub 直连被墙，见踩坑 29）。**手机真机装 PWA 前须先信任 `.https/ca.pem`**（iOS 需另开"证书完整信任"）。
12. **单元测试**：vitest + jsdom，`npm test`（17 个用例：v1 迁移/reducer/游戏化数值/西瓜钟 fake-timers/backupPayload 剥密钥）；`vite.config.js` 已配 `test.environment='jsdom'`。reducer 与 seed 已 export 供测试导入。
13. **lint 口径**：`npm run lint` 0 告警。`.oxlintrc.json` 关闭了 `react/only-export-components`（单文件 SPA 刻意取舍）、`react/purity` 与 `react/set-state-in-effect`（oxc 启发式误报事件处理里的 Math.random / 效果内同步 state；均属刻意模式）。

## 四、长期偏好

- 全程**中文**交流与中文 UI；代码注释用中文、面向"下一个读者"解释约束而非流水账。
- 视觉与体验走**轻松治愈像素游戏风**：暖沙纸底 + 深棕像素描边 + 糖果条纹进度条 + 手绘像素精灵；拒绝冷峻 SaaS 感。
- 游戏化**宽恕优先**：只有奖励、没有惩罚（断签不清零、补打卡无奖励但允许、取消打卡不扣分）。
- 交付尽量做到**零门槛**：能双击打开就不起服务器。
- 踩过的坑要**文档化沉淀**（已有《踩坑指南.md》，新坑继续追加）。
- 重大分叉先问用户（用 AskUserQuestion 给推荐项），其余自主推进、不再反复确认。

## 五、硬性规则

1. API Key 等敏感信息**只存浏览器 localStorage**，不进代码、不进仓库、不外传；请求直连服务商。
2. 不照抄参考项目（日常小镇），只借鉴布局隐喻；命名、文案、美术全部原创（应用名「拾光小镇」、精灵「阿咕」）。
3. `useEffect` 中的 GSAP 入场动画一律 `gsap.fromTo(el, from, to, { overwrite: true })`；禁止裸 `gsap.from`。
4. reducer 自定义 payload 字段**不得使用 `type`**（与 action 类型字段冲突，历史坑：LEDGER_ADD 已改 `dir`）。
5. 一切网络请求：`AbortController` 超时（新闻 9s / LLM 由调用方控制）+ 失败兜底 + UI 明示数据状态（在线/缓存/内置）。
6. 每次改构建配置后，必须用 `file://` 真实双击验证 dist 可用。
7. 尊重 `prefers-reduced-motion`（anim.js 的 `D()` 时长助手 + `gsap.matchMedia` 环境动画）。
8. vite.config 保持 `base: './'` + `viteSingleFile()`。
9. 新增像素画用「字符画字符串 + Canvas 渲染」（src/lib/sprites.jsx），**不引入位图/图标库**。
10. **版本与上传约定（2026-09-08 起）**：每次改动验证（lint/test/build）通过后**自动 commit + push**，不再询问；迭代必须 bump 版本号（新功能/大改 → 次版本 +1 如 `0.2.0`，纯修复 → 修订号 +1 如 `0.1.1`），bump 时同步改 `package.json version` 与 `public/sw.js` 的 `CACHE`（`pixel-town-vX-Y-Z`），提交信息带版本号。

## 六、输出格式

- 对话输出：中文、结论先行、关键路径给可点击的本地链接。
- 代码：React 函数组件 + JSX；状态集中在 store.jsx（reducer + Context）；页面组件放 `src/pages/`，通用组件 `src/components/`，纯逻辑 `src/lib/`。
- 文档：Markdown，中文标题，表格用于枚举型信息。
- 测试：以浏览器真实操作为准（chrome-devtools MCP / computer-use 截图 + evaluate_script），Console 必须零 error。

## 七、已完成工作（全部实测通过）

**工程**：`pixel-town/` Vite 8 + React 19 + GSAP 3 + @gsap/react + vite-plugin-singlefile；中文像素字体自托管。

**代码结构**：
- `src/lib/`：`store.jsx`（全局 reducer + localStorage + 种子数据 + 选择器）、`gamify.js`（XP/金币/奖箱/音效/事件总线）、`sprites.jsx`（字符画→Canvas 像素画引擎：植物 5 阶段×3 种、礼箱、金币、心情脸×5、阿咕、云、太阳等）、`charts.jsx`（像素柱状/折线）、`news.js`（RSS+兜底）、`ai.js`（大模型客户端 + 本地规则智能体，可执行记待办/记账）、`dates.js`、`anim.js`（gsap 配置 + D() 减动效助手）
- `src/components/`：`ui.jsx`（Panel/Btn/Chip/Bar/Modal/Field/Empty）、`effects.jsx`（Toast/彩带/升级弹窗）、`SettingsModal.jsx`（昵称/身高/API 预设/导出导入/重置）、`AgentChat.jsx`
- `src/pages/`：Dashboard、Todos、Ledger、Habits、News、Study、English、Weight、Review（9 页）+ App.jsx 布局（顶栏/侧导航/页面过渡/云朵环境动画/升级检测）
- `src/styles/global.css`：完整像素风设计系统（盒影描边模拟像素圆角等）
- `README.md`（使用说明+规则表）与 `踩坑指南.md`（15 条真实坑：现象→原因→解法）

**游戏化数值**：待办 +10XP/5金币、习惯 +8/3、答对单词 +4/1、记体重 +5/2、复盘 +15/5；每日 XP 里程碑 25/50/80/120 → 礼箱 8/12/16/25 金币；每次获得奖励自动给最缺的植物 +1 生长点（阈值 0/2/5/9/14）；浇水每天首次免费、之后 2 金币；等级曲线 `60+(level-1)*40`；每日 XP 目标 120；8-bit 音效 WebAudio 合成可关闭。

**测试记录**（浏览器逐页实测）：复盘存档（XP/金币/彩带/心情月历/连续天数）✓、待办完成流 ✓、习惯打卡+取消（无惩罚）✓、记账（分类柱状图/流水）✓、新闻在线抓取成功（量子位/爱范儿/InfoQ/HN 当日真新闻）✓、学习记录（+15XP）✓、闪卡翻转/测验/生词本 ✓、体重/BMI/历史 ✓、精灵命令「帮我记一条待办」真实入库 ✓、首页礼箱领取与免费浇水 ✓；Console 零 error；`npm run build` 无警告；dist 单文件 file:// 双击实测渲染完整。

**已修复的真 bug**（详见踩坑指南）：`36氪` 数字开头对象键语法错、RSS 死源与 count 参数、gsap.from 污染（柱状图消失）、体重箭头方向反、精灵正则吞「一条待办」、HN 摘要脏文本、file:// CORS 白屏。

## 八、重要决定及其理由

1. **Vite + React 而非单文件静态页**：用户主动选择，接受 npm 门槛换取可扩展性；同时用 singlefile 弥补了"双击可用"。
2. **智能体双模式**（本地规则兜底 + OpenAI 兼容 API）：无 Key 零成本可用、有 Key 体验升级；本地模式可执行操作（记待办/记账）而不只是聊天；Key 只存本地。
3. **新闻用 rss2json 代理**：浏览器直接 fetch RSS 必被 CORS 拦；免费额度够用；缓存 6h 限流。
4. **替换死源**：机器之心/36氪 RSS 失效 → 量子位+爱范儿+InfoQ+HN（通用源按 AI 关键词过滤标题）。
5. **像素描边用 box-shadow 四向投影 trick**（上/下/左/右各一段投影，角部留空）模拟像素圆角，不用 clip-path/border-image，性能好且易调。
6. **像素画零资源**：字符画+Canvas（image-rendering: pixelated），项目无任何图片文件，风格统一且可版本管理。
7. **「自动浇水」挂在 GRANT reducer 内**：任何奖励都会让最缺的盆 +1 生长点，复刻参考项目"任务完成会自动浇水开花"的隐喻，且无需各页面各自调用。
8. **种子演示数据**：首次打开即有 4 条待办、4 个习惯、12 笔账、15 天体重、2 个学习计划、2 篇复盘——第一印象即"有人气的小镇"。
9. **项目放子目录 `pixel-town/`**（ASCII 名）：规避中文路径对工具链的潜在影响。
10. **升级检测放组件层**（App watch level 变化）而非 reducer 内：保持 reducer 纯函数，副作用（音效/彩带/弹窗）走事件总线。

---

## 九、v2 大版本改造记录（2026-09-06，全项落地）

> 依据「可优化清单」全量实施，逐页浏览器实测通过、Console 零 error、生产单文件构建通过。
> 存档从 v1 → **v2**（`hydrate()` 迁移，旧档自动升级，无需手动处理）。

### 新增模块

| 模块 | 要点 |
| --- | --- |
| 🛒 小镇商店 `ShopModal` | 花盆扩容（60/120）、种子（仙人掌 30 / 小蘑菇 40 / 薰衣草 45，买一次永久解锁 `unlockedKinds`）、装饰（篱笆/稻草人/路灯/池塘）、阿咕帽子（叶子/浆果/皇冠，一次戴一顶 `profile.hat`）。入口在首页花园脚部 |
| 🏛️ 小镇年鉴 `pages/Museum` | 半年 XP 热力图（26 周 canvas，数据 `xpLog`，GRANT 时累加）、植物图鉴（`profile.collection` 记录盛开过的品种）、成就奖杯墙（14 枚，`lib/achievements.js` 纯函数判定，App 层 useEffect 检测解锁） |
| 🍅 番茄钟 `lib/pomo.js` | **模块级单例**（切页继续走）、**时间戳计时**（后台标签页节流也不走慢）、App 层 `PomoBadge` 迷你指示器、完成事件 `pomo-done` 由 App 监听 → POMO_DONE 入库 + 奖励。dev 模式挂 `window.__pomo.startTiny()`（3 秒迷你番茄）供自动化测试 |
| 🌤️ 真实天气 `lib/weather.js` | 设置填城市 → open-meteo 地理编码 + current_weather，WMO 代码映射到 sun/cloud/drop 精灵；缓存 2h（`pixel-town-weather-v1`）；失败/未配置回退伪随机「小镇预言」 |
| ☁️ WebDAV 备份 `lib/webdav.js` | Basic auth PUT/GET 单文件备份。**已知限制：是否可用取决于网盘 CORS**（坚果云实测放行；被拦时 UI 有明示，退回手动导出）。无凭据未实测，交付时按此口径说明 |
| 🔤 TTS `lib/tts.js` | speechSynthesis 发音（点击触发满足手势要求），闪卡/测验/生词本/词库均有 🔊 |

### 存档 v2 变更（`hydrate()` 统一处理 load 与导入）

- `profile.pots`：`[{id:'sunflower',pts}]` → **`[{id:'pot-1',kind:'sunflower',pts}]`**（盆位与品种分离，kind 空 = 空盆）
- 新增：`profile.unlockedKinds / collection / decor / hat / achievements / weightGoal / lastExportDay / stats{todosDone,ledger,pomos}`、顶层 `xpLog / budgets`
- `english.queue`：`string[]` → `[{w,due,interval}]`（简化间隔重复：答对 ×2，≥7 天毕业；答错归零）
- `todos`：新增 `repeat(''|'daily'|'weekly') + lastDone`（重复待办不打 done，翻转 lastDone；`todosOpen/isDue` 已适配）
- `settings`：新增 `city / webdavUrl / webdavUser / webdavPass`
- 导入备份：先 `hydrate` 归一 + confirmBox 显示存档摘要，确认才覆盖

### 其他重要改动

- **阿咕 LLM**：`askAI` 支持 SSE 流式（`onDelta`），非流式网关自动回退；system prompt 约定 `[ACT:todo_add:…]/[ACT:ledger_add:金额:备注]`，`parseActs` 解析后前端执行（本地模式原本就能执行，现在大模型模式也可以）
- **确认框**：`ui.jsx` 的 `confirmBox()`（Promise 风格）+ `ConfirmHost` 替换全部原生 `confirm/alert`；Modal 支持 Esc 关闭
- **键盘快捷键**：数字键 1-9/0/- 切页（输入框聚焦与弹窗打开时不劫持）
- **记账页**：月份切换、收支/分类筛选、分类预算（超支变红）、CSV 导出（带 BOM）
- **体重页**：目标体重（虚线）+ 7 日移动平均（`charts.jsx` Line 支持 `data2/goal`）
- **复盘页**：整月心情月历，可点选历史日期回看/补写（补写不给奖励）
- **页面过渡**：`gsap.from` → `fromTo + overwrite`（HMR 打断防护，见踩坑 19）
- 番茄钟奖励：`min/30*10` 取整钳在 5~30 XP，≥30 分钟 +3 金币

### 实测记录（2026-09-06，Chrome via IAB）

首页成就自动解锁（+25 金币与钱包数核对一致）、真实天气（杭州 25°C）与回退、重复待办完成/撤销与顺延、商店购买三连（金币 158→128→68→48 全对账）、空盆种植、图鉴与成就墙渲染、迷你番茄完成入账、英语四模式（答题奖励、导入解析、错误行跳过）、账本预算（餐饮 90/300 与种子数据吻合）/筛选/CSV、体重目标线、月历点选补写、重置/清空确认框、快捷键与输入守卫、新闻在线抓取 18 条；**Console 零 error**；`npm run build` 无警告，preview 冒烟通过。

### v2 已知限制

- WebDAV 依赖网盘 CORS（坚果云可用；不支持的网盘走手动导出），且本次无真实凭据未做云端实测
- LLM 流式/ACT 用本地规则与单元级验证覆盖（parseActs），未用真实 Key 走完整链路
- file:// 双击验证未重做（构建配置与 v1 完全一致、未动 vite.config，v1 已验证；如后续改构建配置必须补验，见硬性规则 6）

### 本次踩坑沉淀

踩坑指南新增 16-21 六条：**TDZ 声明顺序白屏**、**事件总线发了没人听**、**bloom_sun/bloom_sunflower 精灵名错位（v1 潜伏 bug）**、**HMR 打断 gsap.from 钉死 hidden**、**过渡期 innerText 为空影响自动化**、**bash 双引号吃反引号损坏文档（git 基线救场）**。

---

**请先复述你的理解并指出歧义。**

---

## 十、2026-09-07 迭代交付（优化清单批次一/二/三 + 技术债，4+4+4+4 = 16 项全落地）

> 档案：[待办优化/优化方案.md](./待办优化/优化方案.md)（批次与标杆映射）与 [待办优化/优化清单.md](./待办优化/优化清单.md)（18 条已完成归档 + 本文技术债 4 条）。

### 一批：游戏化深度（6f16696）

- **待办/习惯难度分级**：`🌱简单/⭐普通/🔥困难`，奖励 ×0.6/×1/×1.6（`gamify.js` `DIFFS/rewardBy`），旧档 hydrate 兜底普通；困难完成放礼花。
- **自定义成就**：年鉴「＋ 自定义成就」——计数型（完成待办/番茄/记账/连续/浇灌 + 目标值，App 层 useEffect 自动解锁）或手动心愿（亲手点亮），奖励 0-60 金币，`profile.customAch` 存储、解锁记录并入 `achievements`。
- **商店愿望货架**：自定义现实奖励（文字+定价），金币买下挂架、可兑现/撤销/取下（`profile.rewards/rewardsOwned/rewardsDone`）。

### 二批：洞察与分享（3573e90）

- **每周小结**（复盘页）：按周汇总活跃天/XP/待办/专注/收支 + 7 日 XP 像素柱 + 心情最好的一天，可回看前几周（周一起始，纯计算不落库）。
- **专注分布**（学习页）：近 14 天时长 + 时段（清晨/上午/下午/晚间/深夜）；`POMO_DONE/STUDY_LOG` 会话新增 `h` 小时字段（新记录起算）。
- **像素分享卡**（年鉴 📸）：canvas 生成（等级/累计 XP/连续/金币/四周热力/最近三奖杯）→ 保存 PNG。
- **奖箱盲盒化**：金额 ±30% 随机（`rollChest`），8% 概率开惊喜——解锁未购种子或 +25 金币（`CHEST_BONUS`）。

### 三批：形态进化（2859142）

- **PWA**：`public/manifest.webmanifest + icon.svg + sw.js`（见三·10）；index.html 补 manifest/theme-color/apple meta；`beforeinstallprompt` → **页面安装引导横幅**（Android/桌面一键安装；iOS 无事件则延时 4s 给「添加到主屏幕」手动指引；可关闭存 localStorage、已安装自动隐藏、移动端避开底部 Tab 与 FAB）。
- **番茄钟系统通知**：设置 `settings.notify` 开关（soft opt-in，开启时才 `requestPermission`），专注/休息结束 `Notification`。
- **暗色主题**：`prefers-color-scheme: dark` 全量变量换肤 + 硬编码浅色点修正。
- **移动端**：≤620px 底部 11 Tab 导航 + 页面横向滑动切页（输入框内不响应）+ FAB/徽章避让 + safe-area。

### 技术债（本批，提交随代码仓库）

- **字体子集化**：`scripts/subset_fonts.py` 收集项目字符 → pyftsubset 产出 `src/assets/fonts/*.subset.woff2` + `index.css`（843KB→82KB，dist 1650→612KB，gzip 244KB）。**改动 UI 文案后需重跑该脚本**；运行时接口/用户新输入的中文走系统字体兜底（见踩坑 25）。
- **lint 归零**：28→0（见三·12）。
- **WebDAV 自动备份**：设置「☁️ 每天自动备份」开关 + `profile.lastAutoBackupDay`，每天首次访问自动 `backupPayload`（剥密钥）上传，失败静默次日重试。**云端实测仍待用户真实凭据验证**（坚果云 CORS 放行历史结论保留）。
- **单元测试**：vitest + jsdom，17 用例（见三·11）；`npm test` 一键。

### 交付验证（2026-09-07 全部实测）

- 四轮 Playwright 走查全绿：一批 13 项、二批 16 项、三批 15 项、技术债 10 项（含 `document.fonts.check` 字体断言、自动备份触发到真实网盘请求的 CORS 预期）。
- `npm run lint` 0 告警；`npm run build` 通过（dist/index.html 单文件 612KB + public 三件套）；`npm test` 17/17。
- 旧档兼容：v1 → v2 迁移单测覆盖；最后一轮走查用「无 diff 字段旧档」实测加载不崩、奖励按普通发放。

### 遗留（需用户侧操作）

1. 真实 WebDAV 凭据下验证「自动备份 + 恢复」全链路（含每日留档文件名）。
2. 真实 LLM Key 验证 流式 + ACT 动作（此前以规则/单测覆盖）。
3. 上报线上环境后验证 SW 缓存更新与安装按钮。
4. 手机真机装 PWA 的前置步骤已就绪但未实机验证：信任 `.https/ca.pem` 后访问 `https://<局域网IP>:5173`（dev 端口被占时 vite 自动 +1，按实际端口放行防火墙并访问）。

## 十一、本地 HTTPS / PWA 真机安装配置（2026-09-07 已配通，待真机验证）

> 背景：PWA 的 `beforeinstallprompt` 与 SW 注册在 http 环境不生效，本地用 mkcert 签发可信证书让 dev 服务器直接跑 https，手机同 Wi-Fi 即可体验「添加到主屏幕」。

- `scripts/https-setup.ps1`：自动定位/下载 mkcert（`~\.local\bin\mkcert.exe`，v1.4.4；下载源 GitHub 直连 + ghproxy.net 镜像兜底，见踩坑 29），`-install` 信任根 CA，为 localhost + 全部局域网 IPv4 签发 `.https/cert.pem + key.pem`，并复制 `ca.pem`（给手机安装的根证书）。
- `vite.config.js`：检测 `.https/` 存在 → `server.https = { cert, key }` + `preview` 同步；无证书文件自动退回 http，不影响纯局域网玩法。
- 本轮实测：mkcert v1.4.4 下载成功（4.9MB，经 ghproxy.net）、CA 系统信任 ✓、证书签发成功（有效期至 2028-12-07）、`npm run dev` 输出 `https://<各网卡IP>:5174`、`curl -k` 对 localhost/manifest/sw.js 全部 200。
- 相关文件：`vite.config.js`（https 开关）、`scripts/https-setup.ps1`、`.gitignore` 已忽略 `.https/`（含私钥，严禁入库）。
- 踩坑沉淀：踩坑指南 29-33 五条（GitHub 直连被墙走镜像、单源下载失败会挂掉整个脚本、mkcert 非 PATH 目录查找、SAN 必须含局域网 IP、mkcert stderr 提示被误判为错误），及文末 HTTPS 自查清单。

## 十二、全面检查与修复（2026-09-07）

> 全项目检查：lint 0 告警 / build 612KB 单文件 / 单测 17/17 / 浏览器冒烟 7 页零 console error / 双视角代码审查（lib 与 pages+components）+ 交叉核实。

**修复清单（9 项代码 + 4 处死代码 + 2 处文档）：**

1. **天气定位 reject 逃逸**（weather.js）：拒绝授权/超时的 geolocation Promise 改为 `try/catch return null`，消除 unhandledrejection（踩坑 35）。
2. **跨天今日 XP 不重置**（store.jsx hydrate）：`xpToday/xpTodayDay` 在 hydrate 入口按日归零，跨天首开不再显示昨日进度（踩坑 34）。
3. **导入备份清空密钥**（store.jsx IMPORT）：备份刻意剥 apiKey/webdavPass，导入时对备份为空的两项保留当前配置，密钥不再因恢复丢失。
4. **存档防抖无兜底**（store.jsx AppProvider）：补 `beforeunload` + `visibilitychange(hidden)` 立即落盘，关页不丢最后一步。
5. **自动备份失败风暴**（App.jsx）：失败后加 30 分钟冷却时间戳，不再每次交互都打注定失败的网盘请求（踩坑 36）。
6. **新闻页拉取竞态**（News.jsx）：挂载拉取加 alive 守卫，切页后不 setState/不回滚数据。
7. **英语答题定时器**（English.jsx）："自动下一题" setTimeout 挂 ref，卸载/切模式时清除。
8. **自定义词单内部去重**（English.jsx doImport）：同一 Set 同时滤内置词与粘贴内容内部重复，杜绝重复 key/重复选项。
9. **升级 tooltip 负 XP**（App.jsx）：`Math.max(0, need - xp)` 兜底。

**死代码清理**：store.jsx 未用 `React` import 与 `monthKey` 再导出、effects.jsx 无效 `export { sfx }`、dates.js 未用 `seasonOf`。

**文档同步**：README 踩坑计数 21→33；交接文档三批 PWA 更新为"安装引导横幅"形态；踩坑指南新增 34-36 三条。

### 一键启动脚本（scripts/start-app.ps1 + 根目录「一键启动.bat」）

- 双击「一键启动.bat」→ powershell 执行 `scripts/start-app.ps1`：自动找 5173 起空闲端口 → 新开 cmd 窗口跑 `npm run dev -- --port P --strictPort`（固定端口不跳变，`.https/` 存在自动 HTTPS）→ 探测该端口 200 就绪后用默认浏览器打开 → 脚本退出，dev 窗口保留（关闭即停）。
- 注意：`.ps1` 必须 **UTF-8 带 BOM** 保存，否则 PowerShell 5.1 按 ANSI 解码中文会绞碎语法（踩坑 37）；`.bat` 注释用英文避免 cmd 花屏。
- 验证：真实运行通过（选端口 → 起服 → 200 探测 → 浏览器打开 → EXIT 0）。

## 十三、自主选择植物浇水（2026-09-07）

> 反馈：成长植物园原来只有全局「浇水壶」，系统自动挑「最缺水（pts 最低）」的一盆，用户无法指定给哪株植物浇水。

**改动：**

1. **`WATER` reducer 支持 `potId`**（store.jsx）：指定 `potId` 只浇那一盆；不传则保持自动挑最缺水的一盆；已盛开的盆（`pts >= BLOOM_PTS`）不再参与自动浇水，避免白扣金币。
2. **每盆植物卡片加独立「💧 浇水」按钮**（Dashboard.jsx）：未盛开时显示，点击只浇该盆；已盛开的盆只显示「采集种子」。浇水动画（水滴下落 + 盆弹跳）跟随被点的那盆。
3. **全局「🪣 浇水壶」保留**为自动浇最缺水的盆；空花园/全部盛开的提示语细分。
4. **扣费口径不变**：每天第一次浇水免费（`waterLastDay !== dayKey()` 全局判断），之后每次 2 金币。

**验证**：单测 23/23（新增 3 条：指定盆只浇该盆、不传自动最缺、已盛开不打湿但 waterTotal/扣费正常）；oxlint 0 告警；`npm run build` 614KB 单文件通过。

**坑**：`onClick={water}` 直传带参函数会把 SyntheticEvent 当第一个实参塞进 `potId`，导致全局按钮误报"空盆"——已改 `() => water()` 并记入踩坑 38。

## 十四、游戏化 × 情感化迭代：周报 / 专注墙 / 周挑战 / 昼夜 / 阿咕闪现 / 新手引导（2026-09-08）

> 来源：参考 Forest（视觉收集 × 时间历程）、Habitica（每周挑战）、Todoist（Karma 仪式感）、Fabulous（引导式旅程）等优秀案例，结合本地代码体检提炼；用户圈定 P0 健壮性加固 + P1（专注墙、周报）+ P2（昼夜、阿咕、挑战、引导）。

**已实现：**

1. **🍅 专注番茄田**（年鉴页新面板）：每次专注完成 `POMO_DONE` 记一条 `pomoLog[{t, min, h, planId}]`（上限 200 条自动裁剪），年鉴页以"最近 28 天一格、格内堆小番茄"的可视化墙展示，配累计颗数/小时统计；新增 2 枚成就「番茄田（50 颗）」「夜猫子（21 点后专注）」。
2. **🗞️ 上周小镇周报**（年鉴页顶部新面板）：`lib/weekly.js` 纯函数聚合本周/上周（周一定义为一周开始，`weekKey` 入 dates.js）的完成待办、习惯点亮、专注分钟、记账笔数、复盘篇数、收支、XP，并用 ↗/↘/— 环比箭头对比上周；8 个统计格随数据实时变化。
3. **🔥 每周挑战**（首页新面板）：`lib/challenges.js` 6 个挑战模板（专注 180 分钟 / 15 件待办 / 习惯 20 次 / 账目 10 笔 / 复盘 3 篇 / 连续 5 天），每周一自动 roll 一个（不与上周重复，`profile.weekly` 落档），达成自动发金币（`CHALLENGE_CLAIM` reducer 幂等），首页带进度条。
4. **🌙 小镇昼夜**：`timeOfDay()`（dates.js）按时段在 `body[data-period]` 打标，CSS 变量切换清晨/白天/黄昏/夜晚的天空渐变、山丘、云朵透明度；暗色主题下各时段收敛成对应的暗色变体，不冲突。
5. **🐣 阿咕随机闪现**（`components/AguVisit.jsx`）：最短 3 分钟一见、55% 概率、现身 9 秒说句暖话（数据感知：待办还剩几件）后飘走，点击跳聊天页；尊重 `prefers-reduced-motion`。
6. **🎓 首次启动引导**（`components/IntroOverlay.jsx`）：3 步（欢迎 → 奖励怎么来 → 阿咕是谁），localStorage 记过即不再弹，可跳过。
7. **P0 健壮性加固**：localStorage 写失败 → toast 明说（不再静默丢数据）；`pomo.js` 后台节流补 `visibilitychange/pageshow` tick；`hydrate` 兜底 `pomoLog` 与生词本 `queue` 脏条目健康检查；toast 容器加 `aria-live`、FAB 补 `aria-label`；AI `dataSummary` 防提示词注入（用户内容压单行截断 + system prompt 约束）。

**验证**：单测 34/34（新增 11：POMO_DONE 入 log 且裁剪、hydrate 脏数据踢除、CHALLENGE_CLAIM 幂等、旧档兜底、weeklyReport 基线差、weekKey、challengeOf/roll）；oxlint 0 告警；build 629KB 单文件；浏览器冒烟 7/7 通过（引导 3 步、首页挑战卡、年鉴周报/番茄田、阿咕 FAB、data-period=day、weekly 落档）。

**设计口径（后续迭代记住）：** 周一定义为一周开始（`weekKey`）；所有聚合统计对 `seed()` 保持"相对基线差"断言（踩坑 41/42：seed 已无演示数据，但基线差仍是统计断言的正确写法）；挑战强度是模板常量（`CHALLENGES[].max/coins`）可随手调；`pomoLog` 只记专注完成的 focus 轮（休息轮不记）。

**遗留 / 技术债：** ① 账本分类占比圆环图、WebDAV「从网盘一键恢复」UI（`webdavDownload` 已有能力，只差入口）留待后续；② 周报/周挑战目前只在本地 localStorage 范围统计，跨设备需依赖备份导入；③ `pomoLog` 只保留 200 条，超过后最早的"专注墙"会淡出（有意为之，控制存储）。

## 十五、移除演示数据 · 空白开局（v0.2.0，2026-09-09）

**背景**：用户反馈"没记过账却每次打开都有一堆账、删了又回"。根因是 `seed()` 预置了全套演示数据（12 条账目 + 7 条待办 + 习惯/学习/体重/复盘/24 天 XP 热力图），且 `load()`（无档/坏档回落）、`hydrate()`（`{...base,...parsed}` 缺字段补默认）、跨 origin（http/https、localhost/局域网 IP 的 localStorage 互相隔离）三条路径都会让演示数据"复活"。详见踩坑指南 42。

**改动（全部实测通过）：**

1. **`seed()` 空白开局**（store.jsx）：移除全部演示数据，只保留结构——3 个空花盆（kind 为空）、3 种**免费基础种子默认解锁**（sunflower/tulip/berry，空白开局也能直接种花）、Lv.1/0 XP/0 金币/0 连续、菜谱全空（todos/ledger/habits/study/weights/reviews/xpLog/pomoLog）、阿咕欢迎语保留、settings 不变。
2. **「重置小镇」文案**：从"恢复演示数据"改为"回到最初的空样子"（`RESET → seed()` 语义不变，从此返回的就是干净空档）。
3. **单测同步调整**：weekly.test.js 里原依赖 seed 演示待办的用例改为先自建待办；store.test.js 购买测试先 `PROFILE_SET` 备足金币（空白开局钱包为 0）。34/34 通过。
4. **存量用户处理**：已有 localStorage 档不受影响（`hydrate` 保留旧档）；旧档里残留的演示数据想清掉，去「设置 → 重置小镇」即可得到全新空档。

**验证**：oxlint 0 告警；vitest 34/34；build 626KB 单文件（v0.2.0，sw.js CACHE=pixel-town-v0-2-0）；浏览器冒烟 9 页全绿——`localStorage.clear()` 后首页金币 0/Lv.1/3 空盆可种免费种子、账本全部 ¥0 且无假账、其余各页空态文案正常、无 console error。

**遗留**：跨 origin（http / https / 不同局域网 IP）仍是多份独立存档，不是本次范围；若要做"多端同档"需引入 WebDAV 云恢复入口（`webdavDownload` 能力已具备）。

## 十六、备份方案 A：手动导出 + 坚果云同步（v0.3.0，2026-09-09）

**背景**：用户想用浏览器直连坚果云 WebDAV 做一键备份，实测被 CORS 拦截（`OPTIONS` 无 `Access-Control-Allow-*` 头，`Failed to fetch`，见踩坑 43）。最终选**方案 A**（免费）：手动「导出备份」+ 坚果云客户端同步文件夹，等于"伪云同步"。

**已实现（实测通过）：**

1. **设置页「备份三步走」引导卡**（SettingsModal.jsx）：从未导出过（`lastExportDay` 为空）时，在导出/导入/重置按钮下方显示虚线卡片，教用户三步——① 点导出备份下载 JSON；② 放进坚果云同步文件夹（如 `D:\Nutstore\我的坚果云`）；③ 换设备时导入备份选中该文件。首次导出后自动消失（`EXPORT_MARK` 已设）。
2. **导出成功 toast** 改为提示"放进坚果云同步文件夹，云端就自动有一份"。
3. **首页备份提醒文案**（App.jsx，已有 7 天未导出 toast）更新为指向「导出备份 → 坚果云同步文件夹」。
4. **webdav.js 注释修正**：删除过时的"坚果云实测放行 ACAO"表述，改为 2026-09-09 实测被 CORS 拦截的事实 + 方案 A 指引。
5. 新样式 `.backup-guide`（global.css，虚线边框卡片，深/浅主题均适配）。

**验证**：oxlint 0；vitest 34/34；build 627KB 单文件（v0.3.0，`sw.js` CACHE=pixel-town-v0-3-0）；浏览器冒烟——全新档打开设置，引导卡渲染正常（590×146 无溢出、3 步文案完整、无 console error）。

**遗留 / 说明**：① 「备份到网盘 / 从网盘恢复」两个按钮保留但当前对公共网盘不可用（CORS）；要能用需自建带 CORS 的 WebDAV（AList/Nextcloud/小 VPS），届时把 URL/账号填入同区块即可，无需改代码；② 方案 A 是"周级手动"同步，不是实时双端同步，跨设备修改冲突以最后一次导入为准。
