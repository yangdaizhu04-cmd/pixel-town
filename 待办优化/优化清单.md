# 待办优化清单（未开工）

> 记录时间：2026-09-06
> 说明：这里存放「已经发现、核实过问题、但还没动手做」的优化点。**没有**写入小镇应用的待办功能。
> 状态只有两种：`未做` / `已完成（日期 + 提交号）`。做完一条就把状态改掉并挪到文末归档。

## 清单

### H. PWA 化 【未做】

- 计划（见 优化方案.md 第三批⑧）：manifest + 像素图标 + Service Worker，可「添加到主屏/桌面」独立运行；public/ 输出不破坏 singlefile 离线姿势。

### I. 番茄钟系统通知 【未做】

- 计划（见 优化方案.md 第三批⑨）：专注/休息结束用 Notification API 发系统通知；soft opt-in——设置里手动开启时才请求权限。

### J. 暗色主题 【未做】

- 计划（见 优化方案.md 第三批⑩）：`prefers-color-scheme: dark` 覆盖全部设计变量，顺带修硬编码浅色。

### K. 移动端触屏适配 【未做】

- 计划（见 优化方案.md 第三批⑪）：窄屏底部 Tab 导航 + 横向滑动切页，替代数字键快捷键。

## 归档（已完成）

### 1. 🔐 导出备份剥离密钥 【已完成（2026-09-06 · 7eb45eb）】

- 导出/上传前用 `sanitized()` 构造脱敏副本（`apiKey`、`webdavPass` 置空），备份文件不再含密钥；导入无 Key 的备份一切照旧（离线小精灵兜底）。

### 2. 🐛 英语导入与内置词去重 【已完成（2026-09-06 · 7eb45eb）】

- `English.jsx` 的 `doImport` 先与内置 28 词查重再 dispatch，内置词不再出现两个同名条目。

### 3. 🏷️ 标签页标题随页面/番茄钟切换 【已完成（2026-09-06 · 7eb45eb）】

- `App.jsx` 用 `useEffect` 监听页面与 `pomoSubscribe`：番茄钟运行中标题显示「🍅/☕ mm:ss 专注/休息中」，空闲时显示当前页面标签。

### 4. 🧹 旧已完成待办的数据卫生 【已完成（2026-09-06 · 7eb45eb）】

- reducer 新增 `TODO_CLEAR_ALL_DONE`，今日已完成面板新增「清空全部」按钮（confirmBox 二次确认），历史 `done` 条目可一键清掉。

### 5. 📰 新闻源颜色表清理 【已完成（2026-09-06 · 7eb45eb）】

- `News.jsx` `SOURCE_COLORS` 已移除机器之心、36氪两个死源键。

### 6. ♿ 侧边导航 aria-current 【已完成（2026-09-06 · 7eb45eb）】

- 导航按钮增加 `aria-current={page === p.id ? 'page' : undefined}`，读屏器可播报当前页。

### 7. 📅 习惯页「上一周」导航 【已完成（2026-09-06 · 7eb45eb）】

- `Habits.jsx` 加周偏移 state（0 = 本周），表头「← 更早一周 / 回到本周 →」可回看前几周打卡；未来格子 disabled 判断按日期比较，天然兼容。

> 注：本次同时修复了 `App.jsx` 两行 import 被挤成一行导致的构建报错（vite build 失败），改动已随 7eb45eb 提交。

### 8. 🎚️ 任务难度分级 【已完成（2026-09-07 · 6f16696）】

- 待办/习惯增加「🌱 简单 / ⭐ 普通 / 🔥 困难」难度，XP/金币按难度阶梯缩放（简单 ×0.6、普通 ×1、困难 ×1.6），困难完成放礼花；旧存档 hydrate 兜底默认普通。改动：`gamify.js`(DIFFS/rewardBy)、`store.jsx`、`Todos.jsx`、`Habits.jsx`。

### 9. 🎯 自定义成就 【已完成（2026-09-07 · 6f16696）】

- 年鉴成就墙支持「计数型成就」（完成待办/番茄钟/记账/连续天数/浇灌次数 + 目标值，达标自动解锁，带进度条）与「手动心愿」（亲手点亮）；设 0-60 金币奖励，并入奖杯墙计数，可删除。改动：`achievements.js`、`store.jsx`、`App.jsx`(自动检测)、`Museum.jsx`。

### 10. 🕯️ 愿望货架（现实奖励） 【已完成（2026-09-07 · 6f16696）】

- 商店新增「🕯️ 愿望货架」：自定义文字 + 价格许愿，金币买下后挂在货架激励自己，兑现可打勾/撤销，可取下。改动：`store.jsx`(REWARD_*)、`ShopModal.jsx`、`global.css`。

### 11. 📊 每周小结（自动周报） 【已完成（2026-09-07 · 3573e90）】

- 复盘页新增「每周小结」：按所选周忠实汇总活跃天数 / XP / 完成待办 / 专注分钟 / 收支，配 7 日 XP 像素柱与「心情最好的一天」，可回看前几周（周一起始）。改动：`Review.jsx`、`global.css`。

### 12. 📈 专注分布（番茄钟统计） 【已完成（2026-09-07 · 3573e90）】

- 学习页新增「专注分布」：近 14 天专注时长柱状图 + 时段分布（清晨/上午/下午/晚间/深夜）；番茄完成与手动记录会话新增 `h` 小时字段。改动：`store.jsx`(POMO_DONE/STUDY_LOG 记 h)、`App.jsx`(传 h)、`Study.jsx`。

### 13. 📸 像素分享卡 【已完成（2026-09-07 · 3573e90）】

- 年鉴页「📸 分享卡」一键生成像素风卡片（等级 / 累计 XP / 连续天数 / 金币 / 最近四周热力格 / 最近三枚奖杯），canvas 渲染成功后可保存 PNG。改动：`Museum.jsx`。

### 14. 🎁 奖箱盲盒化 【已完成（2026-09-07 · 3573e90）】

- 冒险礼箱改为盲盒：金币金额 ±30% 随机，8% 概率开「惊喜」——解锁一颗未拥有的种子，全解锁则 +25 金币兜底。改动：`gamify.js`(rollChest)、`Dashboard.jsx`、`store.jsx`(CHEST_BONUS)。

### 15. 🌐 PWA 化 【已完成（2026-09-07 · 2859142）】

- `public/manifest.webmanifest` + 手绘像素 `icon.svg` + `sw.js`（网络优先、断网回退缓存）；`index.html` 加 manifest/theme-color/apple meta；`main.jsx` 注册 SW（仅 http(s)，file:// 静默跳过）；`beforeinstallprompt` 转成设置里的「安装拾光小镇」按钮。改动：新增 `public/`，`index.html`、`main.jsx`、`App.jsx`、`SettingsModal.jsx`。

### 16. 🔔 番茄钟系统通知 【已完成（2026-09-07 · 2859142）】

- 设置新增「🍅 番茄钟结束发系统通知」开关（`settings.notify`）；开启时才申请 Notification 权限（soft opt-in），专注/休息结束发系统通知。改动：`store.jsx`(notify 默认)、`SettingsModal.jsx`、`App.jsx`。

### 17. 🌙 暗色主题 【已完成（2026-09-07 · 2859142）】

- `prefers-color-scheme: dark` 整体覆盖设计变量（纸底/墨色/描边/琥珀全换），并修正 budget-bar、热力图格子、输入框聚焦、导航高亮等硬编码浅色。改动：`global.css`。

### 18. 📱 移动端触屏适配 【已完成（2026-09-07 · 2859142）】

- ≤620px 显示固定底部 Tab 导航（11 项，复用 PAGES，短标签）；页面横向滑动切页（输入框内不响应）；FAB/番茄徽章避让；safe-area 适配。改动：`App.jsx`、`global.css`。

