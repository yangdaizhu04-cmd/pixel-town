# API 接入方案选型

基于项目调性（像素风、治愈系、小镇生活、游戏化生产力）与现有架构（`lib/` 模块 + localStorage 缓存 + 失败兜底），调研并筛选出 4 个契合的免费 API 方案。

---

## 方案一：每日一句古诗词（最契合小镇调性）

**灵感来源**：独立小应用常用「今日诗词」做开场横幅，像素小镇画面+诗词非常治愈，完美搭配现有「昼夜晨昏」小镇氛围。

### 候选 API

| API | 地址 | 特色 |
|---|---|---|
| 诗泉（开源） | `https://poetry.palemoky.com/api/poems/random` | 37 万首，免费无 key，300ms 响应，支持简繁、按朝代/作者筛选 |
| 今日诗词 | `https://www.jinrishici.com` | 按季节/天气/节日/时段智能推荐，最贴小镇场景 |
| 古诗词·一言 | `https://gushi.ci` | 收录近万条名句，json/svg 返回，简单 |

### 落地建议

- 新建 `lib/poem.js`，仿照 `weather.js` 的缓存 + fallback 模式（缓存 24h，离线回退内置诗词）
- 接入点：首页侧边栏「村长的提醒」下方加一张「今日题词」小卡；或每日复盘页顶部
- 契合「每日小镇日报」的仪式感

### 预期收益

成本极低（一个 fetch + 缓存），显著提升首页治愈感和仪式感。

---

## 方案二：农历 + 节气 + 传统节日（日历体验增强）

**灵感来源**：汉字历法类应用把「公历+农历+节气」并列展示，中式生活感强。

### 候选 API

| API | 地址 | 特色 |
|---|---|---|
| 黄历/万年历 | `https://api.timelessq.com/time` | 免费无 key，返回农历、生肖、干支、星座、节日、节气、今日宜忌 |
| UAPI 节假日 | `https://uapis.cn/api/v1/misc/holiday-calendar` | 法定假日+调休+农历，可按年/月/日查 |
| 纯前端库 lunar | `https://github.com/6tail/lunar-javascript` | 不依赖 API，本地算法，1900-2100 |

### 落地建议

- 纯前端引入 `lunar` 库最稳（完全离线，符合 single-file 架构）；API 作为增强数据源
- 接入点：首页抬头加「农历 八月十六 · 寒露」小字；复盘页/习惯页显示「今日节气」；账本页显示「今天是否节假日/调休」
- 节气与花园联动：节气当天花园植物有特殊状态

### 预期收益

中式生活感 + 放假/调休提醒，贴合中国用户习惯。

---

## 方案三：外币汇率（账本模块增强）

**灵感来源**：主流记账 App（如 MoneyWiz、随手记）都支持多币种自动换算；对海淘或出差用户很实用。

### 候选 API

| API | 地址 | 特色 |
|---|---|---|
| Frankfurter（推荐） | `https://api.frankfurter.dev/v2/rates?base=USD` | 无 key、CORS 全开放、201 种货币、历史汇率到 1999 年 |
| 免费汇率 CDN | `https://cdn.jsdelivr.net/npm/@irfanokr/currency-api@latest/v1/currencies/usd.json` | 170+ 币种，jsdelivr CDN，浏览器直连 |

### 落地建议

- Frankfurter 已确认「浏览器直连，无 key 可泄露」，非常适合纯前端架构
- 账本页加一个「汇率换算」小工具：输入外币金额 → 显示折算为记账本位币
- 数据每日更新，缓存 24h 足够

### 预期收益

账本功能边界扩展，海外消费场景闭环。

---

## 方案四：一言 / 励志语录（治愈系氛围强化）

**灵感来源**：一言（Hitokoto）是中文社区最经典的 API，无数个人站用它做启动页/心情签。

### 候选 API

| API | 地址 | 特色 |
|---|---|---|
| 一言 | `https://v1.hitokoto.cn` | 中文社区经典，动画/诗词/哲学/小说分类，服务稳定 |
| 小小API 一言 | `https://v2.xxapi.cn/api/yiyan` | 支持 hitokoto/poetry 分类 |

### 落地建议

- 接入位置多：番茄钟休息幕、分享卡底部、首页「村长提醒」轮换、PWA 空状态
- 与现有 TIPS 数组合并，做成「乡村集市小报」感
- 缓存 24h + 内置 fallback 句子

### 预期收益

低成本全方位提升「治愈感」，数据源是中文社区质量口碑最好的。

---

## 推荐组合

| 优先级 | 方案 | 工作量 | 价值 |
|---|---|---|---|
| ⭐ 高 | ① 每日诗词 | 半天 | 高（调性完美契合，页面立刻有灵魂） |
| ⭐ 高 | ② 农历+节气 | 半天 | 高（中式生活感 + 花园联动） |
| 中 | ④ 一言语录 | 2 小时 | 中（多个场景点缀） |
| 中（可选） | ③ 汇率换算 | 1 天 | 中（账本增强，但非核心） |

---

## 共性注意事项

1. 新建模块放 `lib/`，沿用 `news.js`/`weather.js` 的正经模式：localStorage 缓存 + TTL + 失败 fallback
2. 优先选「无 key + CORS 开放」的数据源，避免把密钥塞进 single-file HTML
3. 优雅降级：离线/API 挂掉时用内置数据，绝不打扰用户