# 拾光小镇 · 像素生活工作台 🌱

一个轻松治愈的像素风单页应用：把待办、记账、习惯打卡、AI 新闻、学习计划、英语练习、体重记录和每日复盘，装进一座会呼吸的小镇。完成任何一件小事都会获得 **XP 和金币**，并**自动给花园里的植物浇水**——让每天的进步都被看见。

## 快速开始

```bash
npm install
npm run dev        # 开发模式，浏览器打开提示的地址（默认 http://localhost:5173）
npm run build      # 产物是单个自包含的 dist/index.html（约 1.6MB），双击即可离线打开
```

> `npm run build` 用了 `vite-plugin-singlefile`：JS/CSS/字体全部内联进一个 HTML，现代浏览器下**双击 dist/index.html 就能用**，不用起服务器（`<script type="module">` 的 file:// CORS 问题已绕开，详见 [踩坑指南](./踩坑指南.md) 第 13 条）。

首次打开自带演示数据，所有数据保存在**浏览器 localStorage**（250ms 防抖自动保存）。设置里可以导出 / 导入 JSON 备份（导入前有确认预览）、配置 WebDAV 云备份、重置小镇。存档自带版本号与迁移逻辑，旧档升级无需手动处理。

## 同步到 GitHub

改完代码想上传时，项目根目录里有一键脚本：

```bash
bash upload.sh            # 自动生成「更新：日期时间」提交信息并推送
bash upload.sh 修了XX问题  # 或用自定义提交信息
```

Windows 下也可以直接**双击「一键上传.bat」**。脚本会自动处理：暂存全部变更、生成提交、推送；远端领先时自动 rebase 重试；没配置 git 身份时自动补默认值。

开发过程中的真实踩坑记录（中文像素字体、GSAP×StrictMode、TDZ 白屏、RSS 存活名单、file:// CORS 等 21 条）见 **[踩坑指南.md](./踩坑指南.md)**。

## 功能地图

| 模块 | 说明 |
| --- | --- |
| 🏠 首页总览 | 真实天气横幅（填城市后接 open-meteo，未填用小镇预言）、五项数据卡、今日冒险进度（25/50/80/120 XP 四个礼箱）、成长植物园（空盆种植 / 盛开采集） |
| 📝 待办清单 | 分类 / 优先级 / 逾期标记 / **重复规则（每天、每周）** / 顺延到明天，完成 +10 XP +5 金币 |
| 💰 收支账本 | 分类像素柱状图、**月份切换、分类预算与超支提醒、流水筛选、CSV 导出** |
| ✅ 习惯打卡 | 一周七格打卡、连续天数火焰、可补打卡（无奖励） |
| 📰 每日 AI 新闻 | 联网抓量子位 / 爱范儿 / InfoQ / Hacker News（rss2json，缓存 6 小时），断网回退内置精选 |
| 📚 学习计划 | 目标小时 + 截止日，**内置像素番茄钟**（切页继续走，完成自动记时长发 XP），按次记录学习时长 |
| 🔤 英语练习 | 闪卡 / 词义测验 / 生词本 / **自定义词单导入**；**TTS 发音**；生词本按**遗忘曲线调度复习**（1→2→4→8 天，连对 7 天毕业） |
| ⚖️ 体重记录 | 像素折线图 + **7 日均值趋势线 + 目标体重虚线**、BMI、逐条历史 |
| 🌙 每日复盘 | 五档像素心情 + 三个小问题，+15 XP，**整月心情月历（可点选补写历史日期）** |
| 🏛️ 小镇年鉴 | **半年 XP 热力图、植物图鉴（收集进度）、成就奖杯墙（14 枚）** |
| 🛒 小镇商店 | 金币水槽：**花盆扩容、植物种子（仙人掌/薰衣草/小蘑菇）、花园装饰、阿咕的帽子** |
| 🐣 小镇精灵「阿咕」 | 离线小精灵模式：查数据、记待办、记账、讲冷笑话；大模型模式支持**流式输出**与**执行动作**（记待办/记账） |

## 游戏化规则（宽恕优先，只有奖励没有惩罚）

- 完成待办 +10 XP / 5 金币 · 习惯打卡 +8 XP / 3 金币 · 答对单词 +4 XP · 记录体重 +5 XP · 复盘 +15 XP · 番茄钟专注 15~60 分钟 5~30 XP
- 每次获得奖励，花园最需要的那盆植物自动 +1 生长点（种子 → 发芽 → 幼苗 → 花苞 → 盛开）
- 浇水壶：每天第一次免费，之后 2 金币一次
- 植物盛开后来「采集种子」换 6 金币、记入图鉴；空盆可种下已解锁的任意品种
- 商店：第四/五花盆 60/120 金币 · 种子 30~45 金币（买一次永久解锁）· 装饰 50~90 金币 · 阿咕的帽子 20~60 金币
- 今日 XP 冲到 25/50/80/120 可开四个奖箱（金币奖励），升级有礼花和 8-bit 号角
- 成就 14 枚（初次冒险、七日之约、初次绽放、满园春色、十个番茄……）解锁送金币
- 连续投入天数：只要当天拿到过任何奖励就 +1，断一天也不会清零羞辱你

## 智能体「阿咕」

- **离线小精灵（默认）**：纯本地规则，能查询待办 / 账单 / 体重 / 学习进度，还能直接执行：
  - `帮我记一条待办：明天交报告`
  - `记账 25 午饭`
- **大模型模式**：设置 → 智能体，选择预设（DeepSeek / Kimi / 通义 / 智谱 / OpenAI）或自定义，填入 OpenAI 兼容接口的 Base URL、API Key、模型名。阿咕会把你的小镇数据摘要注入 system prompt，回答更懂你；支持 **SSE 流式输出**，还能通过 `[ACT:todo_add:…]` / `[ACT:ledger_add:…]` 标记**直接帮你记待办、记账**（前端解析执行）。Key 只存在你的浏览器里，请求直连服务商。

## 天气与数据

- 设置里填常驻城市 → 首页横幅显示真实天气（open-meteo 免费接口，缓存 2 小时；断网/未配置回退「小镇预言」伪随机天气）
- 备份：手动导出 JSON（超过 7 天未备份首页会轻声提醒）/ **WebDAV 云备份**（坚果云等，需网盘支持浏览器跨域）/ 导入前有存档摘要确认
- 账本支持导出 CSV（Excel 直接打开）

## 技术栈

- Vite + React 19 + 原生 CSS（无 UI 框架）
- GSAP 3 + @gsap/react（`useGSAP` 管理页面过渡、数字滚动、礼箱心跳、彩带、闪卡翻转、云朵漂移；入场动画一律 `fromTo + overwrite`，尊重 `prefers-reduced-motion`）
- 中文像素字体：缝合像素字体 Fusion Pixel（npm 自托管分片 webfont，离线可用）
- 所有像素画（植物 6 种、阿咕、礼箱、心情脸、装饰、帽子、奖杯…）都是手绘字符画 + Canvas 渲染，零图片资源
- 8-bit 音效由 WebAudio 现场合成（可在设置关闭）；单词发音用浏览器自带 speechSynthesis

## 目录结构

```
src/
  main.jsx            入口（引入像素字体与全局样式）
  App.jsx             布局 / 导航 / 页面过渡 / 升级与成就检测 / 键盘快捷键 / 番茄钟监听
  styles/global.css   像素风设计系统
  lib/
    store.jsx         全局状态（reducer + localStorage 持久化 + 存档迁移 hydrate）+ 种子数据
    gamify.js         XP/金币/奖箱/音效/事件总线
    achievements.js   成就定义（纯函数判定）
    shop.js           商店目录 / 植物图鉴元数据
    pomo.js           番茄钟（模块级单例，时间戳计时，跨页面存活）
    weather.js        open-meteo 真实天气（地理编码 + WMO 代码映射 + 缓存）
    webdav.js         WebDAV 云备份客户端
    tts.js            单词发音（speechSynthesis）
    sprites.jsx       像素画库（字符画 → Canvas）
    charts.jsx        像素柱状图 / 折线图（支持目标线与第二趋势线）
    news.js           RSS 抓取 + 内置兜底
    ai.js             大模型客户端（SSE 流式 + ACT 动作解析）+ 本地智能体规则
    dates.js / anim.js
  components/         ui（Panel/Btn/Modal/confirmBox）/ effects / SettingsModal / ShopModal / AgentChat
  pages/              Dashboard / Todos / Ledger / Habits / News / Study / English / Weight / Review / Museum
```

## 键盘快捷键

数字键 `1-9`、`0`、`-` 快速切换页面（输入框聚焦时自动失效），`Esc` 关闭弹窗。
