# 🧭 拾光小镇 · 踩坑指南

> 记录这次「像素风生活工作台」开发过程中真实踩过的坑：现象 → 原因 → 解法。
> 希望下一个做像素风 / 游戏化 Web 应用的人能少绕几圈。

---

## 一、中文字体：想要像素感，别信第一个包

### 1. fontsource 的「SC」包不含中文 ⭐ 最意外
- **现象**：`npm i @fontsource/fusion-pixel-12px-proportional-sc` 装完，`index.css` 里只有 `latin-400`，`files/` 目录只有 2 个 woff。中文全部回退成系统字体，像素感直接破功。
- **原因**：fontsource 对 CJK 字体大多只打包了 latin 子集（`sc` 只是家族名后缀，不等于包含简中字形）。
- **解法**：换 `@vp-tw/cjk-web-fonts-fusion-pixel-font`——按 unicode-range 切成 ~79 个分片 woff2，浏览器只按需加载用到的分片，且有真正的简中字形。`import '@vp-tw/.../Fusion-Pixel-12px-Proportional-Simplified-Chinese.css'` 即可，Vite 自动处理相对路径的 woff2。
- **教训**：装完字体先 `document.fonts.check('16px "字体名"')` 或直接看中文渲染，别等截图才发现。

### 2. 字体家族名和你想的不一样
- CSS 里的 `font-family` 是包自己定的（`Fusion Pixel 12px Proportional Simplified Chinese`），打开包里的 css 文件 `head -12` 确认一下再写，别照着 npm 包名猜。

---

## 二、GSAP × React：`gsap.from()` 的隐形坑 ⭐ 本次最经典的 bug

### 3. 连续 `gsap.from()` 第二次终点被污染 → 图表凭空消失
- **现象**：学习计划页的像素柱状图，canvas 绘制正常（`getImageData` 能查到像素），但 `getComputedStyle(canvas).transform` = `scale(0.001)`，柱子永远看不见。
- **原因**：React StrictMode 下 effect 会「跑→清→再跑」。`gsap.from()` 的终点是**创建瞬间读到的当前值**：第一次 `from()` 把元素设成了起点（0.001），第二次 effect 里 `killTweensOf` 后再 `from()`，读到的"当前值"已被污染，动画从 0.001 → 0.001，原地钉死。
- **解法**：凡是写在 `useEffect` 里的入场动画，一律用 **`gsap.fromTo(el, from, to)`** 显式声明终点，并加 `overwrite: true`。Toast、弹窗、聊天气泡全中过招，全部统一改成 fromTo 后才稳定。
- **教训**：gsap-core 文档的 "Do Not" 列表里那条「不要依赖连续 from() 的 immediateRender」不是理论问题，是必踩的坑。一句话口诀：**`useEffect` 里用 `fromTo`，`useGSAP` 的 context 里才放心用 `from`**（context 的 revert 会恢复现场）。

### 4. 数字滚动组件要对字符串"装死"
- 首页统计卡的值有 `¥1,982`、`3/4` 这种字符串，直接丢给 `gsap.to({v: value, snap})` 会变 NaN。Count 组件里先判 `typeof value === 'number'`，字符串直接渲染。

---

## 三、数据源：RSS 江湖比想象中破碎

### 5. rss2json 的 `count` 参数要 API Key
- **现象**：`api.rss2json.com?count=12&rss_url=...` 全部返回 422 "To use this parameter `count` you need a valid api key"。
- **解法**：免费接口去掉 `count`，默认返回 10 条，够用。

### 6. 「大站」的 RSS 早就死了
- **现象**：机器之心（`/rss`、`/rss.xml`）422/500，36氪 `36kr.com/feed` "Feed could not be converted"。
- **解法**：别迷信名单，逐个实测（在浏览器 console 里 `fetch` 一下最真实）。最终存活组合：量子位 ✓、爱范儿 ✓、InfoQ ✓、Hacker News ✓；通用源用标题正则过滤出 AI 相关。
- **附带**：HN 条目描述是 `Article URL: … Comments URL: … Points: …` 模板，直接展示很脏，写个 `cleanSummary` 把前缀剥掉。

### 7. 网络功能必须有兜底 + 明示状态
- 新闻抓取全部失败时回落到内置精选，并在 UI 上诚实标注「📦 内置精选 / 🕊️ 更新于 x/x」——比悄悄失败或假装在线都好。`Promise.allSettled` + `AbortController` 超时是标配。

---

## 四、JS/React 细节：小到容易忽略，大到直接崩

### 8. 对象键以数字开头 = 语法错误
- `{ 36氪: 'gold' }` 直接 PARSE_ERROR "Invalid characters after number"（Vite/Rolldown 把 `36` 当数字字面量）。中文键没问题，**以数字开头的中文键要加引号**：`{ '36氪': 'gold' }`。

### 9. action 字段名撞车
- reducer 里 `case 'LEDGER_ADD'` 用 `a.type` 表示收支方向（in/out），而 `type` 本身是 action 类型字段——两个语义挤一个字段。收支方向改名 `a.dir`。**教训：reducer 的自定义 payload 字段避开 `type`。**

### 10. 正则的分支顺序就是匹配优先级
- `/(?:记(?:一)?(?:条|个|下)?待办|待办[:：]|帮我记)(…)/` 想匹配「帮我记一条待办：给妈妈打电话」，结果「帮我记」这个最短的分支先命中，捕获组吞进「一条待办：」。**最长/最具体的分支放最前面**，或干脆拆成两条正则用 `||` 组合。

### 11. delta 的方向要站在"看的人"那边
- 体重历史里的箭头一开始算的是 `前一天 - 今天`，于是「今天比昨天轻 0.1kg」显示成 `↑0.1`，越看越像胖了。改成 `今天 - 前一天`，`↓` 绿色、`↑` 红色。**数值对、方向反 = 最迷惑用户的 bug。**

### 12. React 受控输入不吃"原生赋值"（自动化测试专用）
- 用脚本填表时，`input.value = 'x'` 不会触发 React onChange。必须：
  ```js
  Object.getOwnPropertyDescriptor(HTMLInputElement.prototype, 'value').set.call(el, 'x')
  el.dispatchEvent(new Event('input', { bubbles: true }))
  ```
  这是 Chrome DevTools MCP / Puppeteer 之外手搓测试的必备技巧。

---

## 五、构建与交付：「双击就能用」比想象中难

### 13. dist 双击打开被 CORS 拦 ⭐ 交付环节大坑
- **现象**：`npm run build` 后双击 `dist/index.html`，Chrome 报 "Access to script … from origin 'null' has been blocked by CORS policy"，白屏。`base: './'` 救不了——**`<script type="module">` 在 file:// 协议下无论相对路径怎么写都会被 CORS 拦**（file 是 unique origin）。
- **解法**：`vite-plugin-singlefile` 把 JS/CSS 全部内联进一个 `dist/index.html`（内联 module 脚本不走 fetch，不受 file:// CORS 限制），字体也转成 `data:font/woff2`。代价是单文件 1.5MB（gzip 约 1MB），换来真正的"双击即用、离线可用"。
- **验证方法**：别只在 http 下测，一定用 `file://` 协议真开一次。

### 14. `npm create vite` 要防交互卡死
- 非交互 shell 里记得带 `CI=1`，否则新版 create-vite 的「Use rolldown? / Install now?」提问会挂住终端。

---

## 六、安全工具的误报要会甄别

### 15. 「弱随机数」警报三连
- 安全扫描（Mimosa）反复标红 `Math.random()` 为"加密用途使用了弱随机数"。实际用途：toast 的 DOM id、彩带喷射角度、随机文案——**没有一处用于加密/鉴权**。安全工具按模式匹配告警，开发者要能判断上下文：如果真是 token/密钥生成，才需要 `crypto.getRandomValues`。

---

## 七、v2 大版本改造：新坑六连（2026-09-06）

### 16. 组件体内 const 声明顺序 → 首屏白屏 ⭐ TDZ 经典款
- **现象**：改造后整个应用白屏，`#root` 空空如也，刷新也没用；控制台只有一条 `Uncaught ReferenceError: Cannot access 'weather' before initialization`。
- **原因**：Dashboard 里 `const wx = weather || fallback` 写在了 `const weather = ...`（派生真实天气那行）的**上面**。`const`/`let` 有暂时性死区（TDZ），声明前引用直接抛 ReferenceError；React 19 渲染抛错且没有 ErrorBoundary 时就是整棵树不挂载。
- **解法**：把 `weather` 的派生挪到 `wx` 之前。ESLint 的 `no-use-before-define` 默认不开，这类错误 lint 抓不到——**白屏先看控制台第一条红字，八成是 TDZ 或 import 环**。

### 17. 事件总线「发了没人听」：计时器走完什么都没发生
- **现象**：番茄钟倒计时正常、切页还在走、迷你指示器正常——但走完之后不记时长、不发 XP、没有任何反应。
- **原因**：`pomo.js` 里 `emit('pomo-done', ...)` 发得很欢，App 层忘了挂 `on('pomo-done')` 监听。事件总线「发后即忘」，没有编译期检查，漏接不报错。
- **解法**：App 挂上监听，完成时 dispatch POMO_DONE + reward + 音效。**每个新事件类型，发出端和接收端要在同一次提交里成对出现**；冒烟测试必须把「触发 → 侧效落库」整条链走一遍，光看 UI 在动是不够的。

### 18. 精灵名按 id 拼接：`bloom_${kind}` 拼出不存在的名字
- **现象**：年鉴「植物图鉴」里已盛开的向日葵只显示文字，图片位置空白，且不报错。
- **原因**：v1 精灵图注册名是 `bloom_sun`（顶栏 logo 在用），而品种 id 是 `sunflower`，按 `bloom_${kind}` 拼出 `bloom_sunflower` 查无此图。`PixelSprite` 对未知名字**静默返回 null**，于是 v1 潜伏、v2 图鉴第一次按品种拼名字时踩中。
- **解法**：`SPRITES.bloom_sunflower = SPRITES.bloom_sun` 一行别名。**「名字拼接」和「名字注册」要用同一份常量**，新精灵注册完把所有拼接点拼一遍自测。

### 19. 自动化测试里"集体隐身"：后台标签页 rAF 停摆（曾误判为 HMR 打断 gsap.from）
- **现象**：自动化测试切页后整块面板一直是 `visibility: hidden`，gsap 补间 `progress()` 卡在 0，截图和 DOM 结构却"一切正常"。曾一度复现失败、误判成"HMR 打断 gsap.from"，干净加载后"无法复现"其实就是因为那时页面在前台。
- **原因**：浏览器对**不可见的标签页完全不派发 requestAnimationFrame**，而 gsap 的 ticker 靠 rAF 驱动 → 所有补间冻结在当前帧。页面过渡的 fromTo 有 `immediateRender`，创建瞬间就把起点（含 `visibility: hidden`）同步写进行内样式，rAF 不来就永远停在起点。**真实用户看着页面时不存在此问题**（前台 rAF 正常），纯测试环境假象。
- **鉴别方法**：`gsap.ticker.time` 采样两次不前进 + `document.visibilityState === 'hidden'` 即可实锤。
- **解法**：测试时先把页面置前台（或把动画相关断言改成不受 visibility 影响的 `textContent` DOM 断言）。`fromTo + overwrite` 的加固仍保留——它本就是硬性规则 3 的要求，只是与本条根因无关。

### 20. 自动化测试读 innerText 拿到空串：页面过渡期的隐藏元素
- **现象**：切页后立刻用 `el.innerText` 读内容（或用 Playwright 点按钮），拿到空串 / 等待超时，但 DOM 里明明有文本。
- **原因**：入场动画的 autoAlpha 起点把元素设为 `visibility: hidden`，而 **innerText 只统计「被渲染」的文本**，hidden 元素返回空串；Playwright 的可交互判定同样会等元素可见。
- **解法**：测试代码在切页后等 ≥1.5s（动画 0.42s + stagger）再断言，或改用不受可见性影响的 `textContent`。同理，驱动 React 受控输入要用原生 setter + `dispatchEvent(new Event('input', { bubbles: true }))`。

### 21. 往含反引号的 Markdown 里写内容：bash 双引号会吃掉反引号
- **现象**：用 `python -c "..."`（双引号包裹）往本文件追加带反引号代码片段的段落，shell 把反引号当命令替换执行，写出的文件内容被绞碎。
- **解法**：改内容一律用编辑工具（Write/Edit）直接写文件；必须在 shell 里拼长文本时用单引号或 heredoc。git 基线提交立了功——`git checkout -- 文件` 一秒恢复。

### 22. 像素帽子别用 CSS 叠加：错位、出画、显丑 ⭐ 来自用户的第一手差评
- **现象**：给阿咕戴帽子时，用两个 canvas + `position: absolute` 叠加，帽子悬浮在头顶偏上、和 FAB 边缘打架，用户直言「建模太丑了」。
- **原因**：CSS 叠加的定位基准（`top: -26%` / `left: 50%`）对「字符画里头部实际占哪几列」毫无感知，不同精灵、不同缩放下都会漂；像素风对 1px 错位零容忍。
- **解法**：**在字符画层面合成**——把帽子行拼到鸟的行数组顶部（`[...hatRows, ...birdRows]`），一张 canvas 天然对齐，缩放永远一致。教训：像素美术的「叠放关系」应该在数据层（字符画）解决，不要在布局层（CSS）解决。

### 23. 完成「逾期待办」后整条凭空消失 ⭐ 全面检查（Playwright 全页面走查）抓到的真 bug
- **现象**：一条逾期（day < 今天）未完成的待办，点完成之后既不出现在「今日已完成」，也不在清单里——界面上彻底找不到，也没法取消。
- **原因**：`TODO_TOGGLE` 只翻转 `done`，不动 `day`。待办本体留在数组里（`done:true, day:昨天`），但 `todosOpen` 过滤掉已完成、`todosDoneToday` 只认 `x.done && x.day === 今天` → 两条漏斗都不接它，数据「隐身」且无入口操作（只能等「清空全部已完成」一次清掉）。
- **解法**：非重复待办完成时把 `day` 一并置为今天（`day: nowDone ? t : x.day`），让完成动作天然归属「今日已完成」。
- **教训**：只测「正常当天完成」这条路，漏了「逾期补完成」的分支；**日期兜底项（day 默认 t）要当成不变量去写，别让中间态落进任何过滤器的空档**。

### 24. `concat` 出重复 key：同 id「其他」在两个分类数组里各有一份
- **现象**：账本流水页控制台报 `Encountered two children with the same key, "其他"`。
- **原因**：`CATS_OUT` 和 `CATS_IN` 都以 `{ id: '其他' }` 结尾，`CATS_OUT.concat(CATS_IN).map(key={c.id})` 产生两个 `其他` 键。
- **解法**：map 时带下标合成 key（`key={`${c.id}-${i}`}`）。教训：**任何 `.concat(...)` 之后当 key 的 id，别假设两边不重名**；重复 key 不报错只警告，但会让复用组件的行为飘。

### 25. 字体子集化：不装 brotli 跑不动，格式拼重复全盘 error ⭐ 字体体积 843KB→82KB
- **现象**：① `pyftsubset` 处理 woff2 报错；② 用脚本生成子集 @font-face 后，浏览器里 `document.fonts` 的 78 个 face **全是 `status: error`**，中文静默回退系统字体，控制台却一个报错都看不到。
- **原因**：① fontTools 读写 woff2 依赖 **brotli**（miniconda 里没装，`pip install brotli` 解决）；② 生成 CSS 时用正则替换 `url(...)`，却没把原声明尾部自带的 `format("woff2")` 一起吞掉，拼出 `url(...) format("woff2") format("woff2")`——非法声明，Face 构造即失败。
- **解法**：调用改成 `python -m fontTools.subset`（避免总要找 Scripts 下的 exe）；替换时整段匹配 `url\([^)]*\)\s*format\("woff2"\)`。**判断字体是否真的生效，用 `document.fonts.check('16px "家族名"', '测试字')` 而不是肉眼看**。
- **沉淀**：交付物 `scripts/subset_fonts.py`（读节点里原包分片 → 收集项目源码全部字符 → 各自子集到 `src/assets/fonts/`）一键复现；改文案后跑一遍即可。经验：子集化 CJK 字体最怕「面板上看着对、实际 glyph 缺失」，务必用 `document.fonts.check(带示例文字)` 断言。

### 26. PWA 的 Service Worker 会"粘住"旧页面 ⭐ 开发期最容易被绕晕的坑
- **现象**：`sw.js` 网络优先 + 失败回退缓存后，改完代码 dev 里刷新仍是旧界面（缓存命中），或线上更新后旧窗口一直用老脚本。
- **原因**：SW 生命周期：新 SW 要等当前页面全部 tab 关闭后才接管；且 fetch 回退走的是缓存那份 `index.html`。
- **解法**：`activate` 里清旧缓存（本作已做）+ `skipWaiting()`/`clients.claim()`；**只注册于 http(s)**，file:// 双击打开时 SW 静默跳过（不影响 singlefile 离线姿势）；开发阶段被 SW 困住时，在 DevTools 的 Application → Service Workers 里 Unregister + 勾选 Update on reload，或顺手换端口。**dist 里 `sw.js/manifest.webmanifest/icon.svg` 属于 public/ 会随构建输出，但 singlefile 不会把它内联**——线上部署才需要它们，双击 index.html 的用法不依赖这几个文件。

### 27. Notification 只能在「用户手势」里申请权限
- **现象**：页面加载完想静默请求通知权限，直接 `Notification.requestPermission()` 被浏览器无视（Chrome 要求由点击等手势触发），或在 jsdom/headless 里没有 Notification 可用。
- **解法**：把申请挪到用户主动动作里——设置里"🍅 番茄钟系统通知"开关 on 时才申请（soft opt-in，`onNotify` 里 `requestPermission`）；自动化测试用 `addInitScript` 注入 stub（`class FakeNotif { static get permission(){...} }`）代替真 API。

### 28. 多文件并行改完后必做「落盘复核」
- **现象**：本批次多次出现「看到编辑结果回显成功，但 grep/lint/build 一跑，某几处改动其实没落盘」（import 没进、函数定义丢了、CSS 变量没生效），页面随之白屏/报 undefined。
- **解法**：**每个文件改完后用 Grep 复核定案**，再做 `lint → build → 浏览器走查` 三道闸；报 `xxx is not defined` 时先 `grep xxx` 在当前文件里找引用和定义是否成对存在，十有八九是"引用在、定义丢"的编辑事故。
- **教训**：回显 ≠ 落盘；以 Read/Grep 读出来的内容为准。

---

## 八、本地 HTTPS / PWA 真机安装：新坑五连（2026-09-07）

### 29. GitHub 直连被墙：api 能连、release 下载超时 ⭐ 网络环境新常态
- **现象**：`Invoke-WebRequest` / `curl.exe` 直连 `github.com/.../releases/download/...` 全部 `Connection timed out`，但 `api.github.com`（拿 release 元数据）能正常返回 200。
- **原因**：本地网络把 `github.com`（及 release 重定向域）的传输层拦了，而 `api.github.com` 不在拦截名单；`browser_download_url` 指向的恰好是被拦的域，所以「API 能拿到 URL ≠ 能下到文件」。
- **解法**：走 GitHub 加速镜像 `https://ghproxy.net/<原始URL>`（实测对 mkcert v1.4.4 windows-amd64 可用，4.9MB 秒下）；`ghfast.top` / `mirror.ghproxy.com` 本次都不通，多备几个镜像源逐个试。
- **验证**：下载完成后 `mkcert -version` 确认 v1.4.4（避免代理给你下个 HTML 假文件——按 0 字节/HTML 内容判断）。

### 30. PowerShell 脚本里内联下载长 URL：单个源挂了整个脚本跟着挂
- **现象**：`https-setup.ps1` 只在 GitHub 直连一个地址下载 mkcert，直连超时后 `$ErrorActionPreference='Stop'` 直接把脚本抛退出，openssl 兜底也没走到。
- **解法**：把候选 URL 放数组，`foreach` 逐个 try/catch，全挂才回退 openssl；脚本被"卡在下载"比"没有证书"更难被用户发现。

### 31. mkcert 装到 PATH 之外的目录：脚本找不到它
- **现象**：mkcert 手动下载到 `~\.local\bin\mkcert.exe`（不在 PATH），`Get-Command mkcert` 返回空，脚本二话不说又开始下载。
- **解法**：脚本的查找候选列表补上 `$env:USERPROFILE\.local\bin\mkcert.exe` 等常见非 PATH 位置。

### 32. 本地 HTTPS 证书的 SAN 必须含「手机要访问的那个局域网 IP」
- **现象**：证书只签了 localhost，手机用 `https://<电脑局域网IP>:5173` 打开时证书校验失败（Chrome 报 NET::ERR_CERT_COMMON_NAME_INVALID）。
- **解法**：用 `Get-NetIPAddress` 收集全部 IPv4（排除 169.254 与 127.0.0.1）塞进 SAN；多网卡（VMware/WSL/热点）环境一定要把**实际 WLAN/以太网 IP** 也覆盖到，用 `curl -k` 之前先确认 IP 列表。**换网段后证书要重跑脚本重签**。

### 33. mkcert `-install` 的 stderr 提示会被 PowerShell 误判成错误
- **现象**：`& $mkcert -install 2>&1` 输出 "The local CA is already installed in the system trust store! 👍"，PowerShell 5.1 把 stderr 通道内容包装成 NativeCommandError 红字，仿佛失败。
- **解法**：`2>&1 | Out-Null` + try/catch 吞掉普通提示；`-CAROOT` 拿真实根目录再 `Import-Certificate` 到 `Cert:\CurrentUser\Root` 兜底（写 LocalMachine 需要管理员开不起）——外部命令往 stderr 打"正常消息"时，判断成败要看 ExitCode 而非报错样式。

### PWA 本地 HTTPS 就绪后自查清单（2026-09-07 验证通过）
- vite 用 `server.https = { cert, key }`（vite.config.js 检测 `.https/` 存在即启用，不存在自动退回 http，改回纯局域网玩法也不受影响）。
- `curl -sk https://localhost:5173/` → 200 且 `ssl_verify_result` 为 0；manifest / sw.js 各 200。
- 手机同 Wi-Fi 访问 `https://<局域网IP>:5173` 前先给手机装 `.https/ca.pem`（iOS 还要在「关于本机→证书信任设置」开启完整信任）。
- dev 端口被占时 vite 自动 +1（5173→5174），防火墙放行规则要按实际端口改。

---

## 九、全面检查修复：四类"不会崩但会错"的隐患（2026-09-07）

### 34. 按天的运行时状态只在新动作里重置 → 跨天首开显示"昨天的今天"
- **现象**：`xpToday / xpTodayDay` 只在 `GRANT`（发奖励）分支里按天重置。用户昨晚拿到 120 XP 后关掉页面，今天一早打开：首页「今日冒险进度」显示 120/120（昨天数据）、四个礼箱却全可开——进度和可开状态自相矛盾，直到今天第一次拿奖励才被纠正。
- **原因**：跨天逻辑只在"有新数据写入"时触发，而"只读展示"的打开瞬间没有触发点。
- **解法**：把"按天归零"下沉到 `hydrate()`（load 和 IMPORT 共用入口）：`xpTodayDay !== dayKey()` 时 `xpToday=0`。**凡是"今日"派生状态，重置逻辑不能只在写入分支，必须在所有进入应用的路径（加载/导入）都过一遍**。
- **检查方式**：用"跨天打开页面"的用例验证（把存档里 `xpTodayDay` 改成昨天再加载），别只测"当天连续操作"。

### 35. Geolocation 的 Promise reject 会逃逸成 unhandledrejection ⭐ 浏览器 API 的失败路径常藏在 async 之外
- **现象**：设置里开「跟随定位」，用户点了「拒绝授权」（或 8s 定位超时），控制台出现 unhandled promise rejection；`fetchWeatherByGeo` 返回了 reject 的 Promise 而非 `null`。
- **原因**：`new Promise(...getCurrentPosition(resolve, reject))` 那段写在了函数里唯一一个 `try/catch`（包 fetch 用）的外部。函数注释写着"失败一律返回 null"，但定位这一步的异常根本没被兜住；调用方 `job.then(...)` 又没挂 `.catch`。
- **解法**：整个定位流程套 `try { … } catch { return null }`，与 fetch 部分共用"失败即回退小镇预言"的口径。**教训：async 函数里的"前置 Promise"（定位/权限/计时器）最容易漏 catch，写完数一遍函数里每一处 await 是否都在 try 内。**

### 36. 自动备份失败"每次都重试"= 每次交互都在给注定失败的网盘请求送热
- **现象**：WebDAV 地址/CORS 配置有问题时，用户每做一次操作（打勾、记账、切页）就触发新一轮自动备份请求——界面没打扰，但请求风暴一直在后台打。
- **原因**：失败后 `.catch` 把"当日已尝试"标记清空，而 effect 依赖整个 `state`，下一次任意 dispatch 就重跑整段"尝试备份"。
- **解法**：失败后写入**冷却时间戳**（30 分钟内不再自动重试），成功仍按日去重（`lastAutoBackupDay`）。**"失败后重试"和"每次状态变化都重试"之间要有一道时间闸，否则失败场景就是无限重试循环。**
- **连带**：同轮修复——① 导入备份会清空已配 API Key/网盘密码（备份刻意剥密钥，导入时对空密钥保留当前配置，见交接文档三·10 口径）；② 存档 250ms 防抖保存补 `beforeunload/visibilitychange` 兜底，关页/切后台不丢最后一步；③ 新闻页拉取补 alive 守卫防切页竞态；④ 英语答题的"自动下一题"定时器挂 ref，卸载时清除。

### 37. PowerShell 5.1 按 ANSI 解码脚本：UTF-8 无 BOM 的中文会绞碎成语法错误 ⭐ 写 .ps1 必看
- **现象**：新写的 `scripts/start-app.ps1`（编辑器保存为 UTF-8 无 BOM，含中文注释与控制台提示）一运行就报 `} catch {` 的 `catch` 不是有效 cmdlet / `Unexpected token '}'`，且输出乱码——明明单看代码结构没问题。
- **原因**：Windows PowerShell 5.1 读取 `.ps1` 时不认 UTF-8 无 BOM，按系统 ANSI 代码页（GBK）解码，中文字节序列变成乱码甚至吞掉换行，把 try/catch、if/else 的语句边界绞坏；报错位置的"行号"也已失真，容易误判是逻辑问题。
- **解法**：脚本文件必须存为 **UTF-8 带 BOM**（或 UTF-16 LE）。给现有无 BOM 文件补 BOM 一行搞定：
  ```powershell
  $c = Get-Content -Raw .\scripts\start-app.ps1
  [System.IO.File]::WriteAllText(<绝对路径>, $c, [System.Text.UTF8Encoding]::new($true))
  ```
  顺手两个配套约定：**`#!/usr/bin` 场景同理会踩**；PowerShell 里注释一律用 `#`，C 风格 `/* */` 块注释不解析。`.bat` 里的注释同理，中文系统 cmd 下 UTF-8 中文会花屏——批处理注释也建议英文。

### 38. `onClick={handler}` 直传带参函数：事件对象会被塞进第一个参数 ⭐ 加参函数的经典翻车
- **现象**：给植物园做"按盆浇水"时，把浇水函数从 `water()` 改成 `water(potId)` 后，全局「浇水壶」按钮写的是 `onClick={water}`——结果一点按钮就提示"这个盆还是空的"，明明花园里有植物。
- **原因**：React 会把 `SyntheticEvent` 原样作为第一个实参传给 handler。`potId` 收到的其实是事件对象，`state.profile.pots.find((x) => x.id === potId)` 永远找不到 → 走"空盆"分支。
- **解法**：凡是带参 handler 一律包箭头函数 `onClick={() => water()}`（不带参时），或 `onClick={() => water(pot.id)}`（带参时）。**带参函数绝不能裸传 `onClick={water}`；`onClick={(e) => fn(e)}` 这种把 e 继续往业务函数里塞的写法同样要警惕。**
- **连带**：本次给 `WATER` reducer 增加 `potId` 字段支持按盆浇水：指定 `potId` 只浇那一盆，不传则自动浇最缺水（pts 最低）的一盆；已盛开的盆不再被浇水（`pts < BLOOM_PTS` 过滤），避免浪费金币。UI 层每盆植物卡片新增独立「💧 浇水」按钮，全局「🪣 浇水壶」保留为自动浇最缺水的盆。配套 3 条单测覆盖"指定盆 / 自动最缺 / 已盛开不打湿"。

### 39. 大批量编辑一个组件文件后，中途某次 import 会“凭空消失” ⭐ 用冒烟测试兜底
- **现象**：这一轮给 `App.jsx` / `Dashboard.jsx` 加新功能，分了好几批 Edit 提交。Browser 冒烟时先报 `showIntro is not defined`，修完又报 `challengeOf is not defined`——检查源文件确实没有那两行 import（明明编辑成功时显示过）。
- **原因**：同一文件在短时间内的多轮编辑，若编辑前后内容 version 对不上（比如后面某次编辑的 old_string 匹配到的其实是更早的版本），可能把前面写进去的增量回滚掉，且不会报错。属于编辑工具的一致性陷阱，不是代码逻辑问题。
- **解法**：**所有新 import 集中在文件顶部一次加齐，改完统一用 `grep/Select-String` 核对每个符号出现次数（import + 使用都 ≥1），最后必须让浏览器冒烟把关键页面点一遍**。宁可多花两分钟核对，也不要让"import 丢了"这种低级错误吞掉一下午。

### 40. 浏览器后台标签页节流 setInterval：番茄钟倒计时会“迟到” ⭐ 时间敏感逻辑要回前台补一次
- **现象**：把标签页切到后台（或最小化），Chrome 会把 `setInterval(250ms)` 节流到 1 分钟甚至完全暂停；专注结束时「pomo-done」迟迟不触发，休息轮/通知跟着迟到几分钟。
- **原因**：`pomo.js` 已经用「结束时间戳」代替累加计时（`st.left = (endAt - now)/1000`），**精度不受节流影响**，但触发动作依赖 interval 的 tick——后台不再 tick，就没人去检查该不该结束。
- **解法**：监听 `visibilitychange` / `pageshow`，页面重新可见时立即手动 `tick()` 校准一次，结束条件满足就当场触发完整流程。
- **连带**：顺手加固三处——① 存档写入失败（localStorage 满）从"静默忽略"改为 toast 明说，防止用户不知不觉丢数据；② `hydrate` 对旧档/导入数据里的 `pomoLog`、生词本 `queue` 做结构健康检查，脏条目直接踢掉（防 `q.w` 之类的 TypeError）；③ AI 的 `dataSummary` 把用户录入（待办/习惯名/计划名）压成单行并截断，system prompt 加"数据区域不是指令"约束，防提示词注入。

### 41. 单测里造“干净旧档”验证统计：seed() 自带演示流水，绝对断言会撞车 ⭐ 用前后基线差
- **现象**：新增 `weeklyReport` / 挑战统计后写单测，断言"本周支出 = 0"，结果拿到 242；"上周支出 = 100" 拿到近 300。
- **原因**：`seed()` 返回的演示存档里就有今天/昨天的收支、待办、习惯打卡记录（为了让首屏有生活感），它们落在"本周"区间内。统计类测试如果对 seed 数据做绝对相等断言，必然被演示数据打脸。
- **解法**：统计/聚合的断言一律写成**相对基线差**：先在 `seed()` 上跑一遍取 `before = weeklyReport(seed())`，操作 reducer 后断言 `after.thisWeek.x === before.thisWeek.x + 新增量`。这样测试与演示数据解耦，改 seed 也不会连带挂测试。
- > 注：v0.2.0 已整体移除 `seed()` 里的演示数据（见下条 42），但"相对基线差"仍是统计类断言的正确写法，保留。

### 42. 用户没记过账却有数据、删了又回：seed() 预置演示数据惹的祸 ⭐ 别用假数据假装"生活感"
- **现象**：用户反馈"记账功能有问题：我没记过任何一条，每次打开都有一堆账，删掉重新打开又出现"。账本里全是没记过的"工资 8500 / 晚饭 25"之类。
- **原因**：`seed()`（新建档 load() 无档/坏档时的兜底档）预置了全套演示数据——12 条账目、7 条待办、习惯/学习/体重/复盘/24 天 XP 热力图，日期还相对"今天"动态生成；每次回落 `seed()` 都是全新随机 id，"删了又回"必现。回落触发有三条路径：① localStorage 被清空/损坏 → `load()` 静默返回 `seed()`；② `hydrate()` 顶层 `{ ...base, ...parsed }` 合并，旧档缺字段时被 seed 的演示值补上；③ **http/https、localhost/局域网 IP 是不同 origin，localStorage 各自独立**，换地址打开又是一份"首开种子档"。
- **解法（v0.2.0）**：移除 `seed()` 里全部演示数据，改为空白开局（只保留结构、3 种免费基础种子解锁、阿咕欢迎语）；「重置小镇」文案从"恢复演示数据"改为"回到最初的空样子"。**教训：演示数据只能用于开发联调，绝不能成为正式存档的默认值——用户分不清"我的数据"和"假数据"，且任何一次存档丢失都会制造"数据复活"的错觉。**
- **验证**：`localStorage.clear()` 后浏览器冒烟 9 页全绿（账本 ¥0 无假账、花园 3 个空盆可种免费种子、各页空态文案正常、无 console error）；单测 34/34、oxlint 0 告警、build 通过；依赖演示数据的单测同步改为自造数据。

### 43. 公共网盘 WebDAV 浏览器直连被 CORS 拦截：坚果云实测失败 ⭐ 别把"实测放行"当永久事实
- **现象**：设置里按文档配好坚果云 WebDAV（地址/账号/应用密码都对），点「备份到网盘」只弹 `Failed to fetch`——不是 401 也不是 404，界面提示 "若是 CORS 拦截……"。
- **原因**：浏览器 `fetch` 跨域到 `dav.jianguoyun.com` 前会先发 `OPTIONS` 预检；2026-09-09 用 `curl -X OPTIONS` 实测响应是 `HTTP/1.1 401` 且**没有任何 `Access-Control-Allow-*` 头**，浏览器直接判定非法跨域、请求发不出去，所以真实状态码都到不了 UI。源码里"坚果云实测放行（Access-Control-Allow-Origin: *）"是早期结论，如今坚果云策略已关闭——**公共网盘的 WebDAV 普遍限制浏览器直连**。
- **解法**：① 想"应用内一键备份"要有自建带 CORS 响应头的 WebDAV（AList / Nextcloud / 小 VPS），代价见交接文档；② 零成本方案 A——点「导出备份」下载 JSON（不含密钥），放进**坚果云客户端同步文件夹**（如 `D:\Nutstore\我的坚果云`），客户端自动上传云端；换设备「导入备份」选中该文件即可，等于"伪云同步"。v0.3.0 已把引导做进应用：设置页未导出时显示「备份三步走」引导卡、超过 7 天未导出首页弹提醒。
- **教训**：任何"外部服务愿意配合"的假设（CORS 放行、API 口径、RSS 存活）都会过时，交付前必须重新实测一遍，并同步修正源码注释——老注释里的"实测放行"会误导后来人。

---

## 十、每日诗词接入：接口选型以"实测响应体"为准（2026-09-11）

### 44. 老牌 API 可能已变成 SPA 页面：gushi.ci 返回 HTML 而不是 JSON ⭐ 验收又过时了
- **现象**：按方案文档的候选名单去测 `https://gushi.ci/all.json` 等地址，返回的 `Content-Type: text/html`，是一个 HTML 首页（含 `saved from url` 痕迹），根本没有 JSON。
- **原因**：这类"老牌免费接口"经过了改版，往后端请求会先被回落到前端 SPA 页面，不再直接吐数据（或接口地址早换）。
- **解法**：`curl -D -` 看响应头先确认 `Content-Type: application/json` 再谈解析；命名里挂着 `.json` 的 URL 也可能是 200 的 HTML。别迷信方案文档里"调研过可用"的结论，交付前逐条重新实测。
- **教训**：和踩坑 43 同源——**外部服务的真实状态会漂移**，"当年可用"不等于"今天可用"。

### 45. 终端 curl 中文乱码不代表接口坏：先看 charset，别被 mojibake 吓退
- **现象**：`curl.exe` 直连诗泉成功后，`content` 数组、`author.name` 里全是 `白居�?` 这种乱码，第一反应是接口/编码坏了。
- **原因**：Windows 终端按 GBK 解码 UTF-8 字节流导致显示乱码；响应头是 `Content-Type: application/json; charset=utf-8`，浏览器 fetch 会正确解出。"显示乱"和"数据坏"是两回事。
- **解法**：遇到乱码先看 `-D -` 响应头里的 charset；确认是 utf-8 就直接写针对字段的解析代码，最后用浏览器 console（或给页面加临时代码）复核真实显示，不要因为终端花屏就去换 API。

### 46. 免费 API 的"包一层"响应：解析要写对"真实返回结构"而不是假设的
- **现象**：诗泉 `/api/poems/random` 实测返回 `{ data: { id, title, content: string[], author: { name }, dynasty: { name } } }`——不是扁平对象，`content` 还是**数组**（整首诗逐行存）。
- **原因**：没实测前容易按"标题+诗句字符串"的直觉解析，`raw.data` 这层壳和 `content` 数组一漏，`toPoem` 就返回 null，功能会静默回退到兜底。
- **解法**：按实测结构写解析（`raw.data` → `content.map(trim).filter(Boolean)` 取前两行做"题词"，超长退化成一行），并用 7 条单测钉住：正常解析 / 同日二次走缓存 / 跨天换新 / fetch 抛错回退 / 非 200 回退 / 结构异常回退 / 兜底条目完整性。
- **附带**：每日一首靠"缓存 key 里带日期 + 24h 有效期"实现，离线兜底用 `hashOf(day + ':poem')` 做到同一天稳定、跨天轮换——和每日天气同一套路，只是换了个 salt。

---

## 复盘一句话

> **"先实测假设，再写进代码"**——字体子集、RSS 死活、file:// 行为、GSAP 双跑时序，四个大坑全是"我以为"和"实际是"的差距。写完立刻在真实环境里点一遍，永远比事后review省时间。
>
> v2 补充：**"发了的事件要有人接，拼出来的名字要真的存在"**——白屏（TDZ）、静默失败（无人监听 / 精灵缺失）都不会把错误送到脸上，只有把整条链真实点一遍才能兜住。
>
> 全面检查补充：**"给所有『完成/归档/删除』类操作写可见性断言"**——记录类操作（记账/体重/习惯）只验证「写进去了」不够，还要验证「完成之后它出现在哪里、还能不能撤销」。用 headless 浏览器把 11 个页面逐页点一遍、顺带收集 console.error，能抓到 lint 和代码审查都看不见的真 bug。
>
> 优化补充（第二轮）：**"跨模块要共享的常量，按依赖方向决定住在哪一层"**——内置英语词库从页面组件抽到 `lib/words.js`，让 store 也能引用做导入去重而不产生「页面→store→页面」的循环依赖；同理 `habitStreak` 加参考日期参数，让周视图能算"当周末"的连续而不是今天的。
>
> 技术债补充（2026-09-07）：**"把验证写进脚本，把结论写进文档"**——字体子集化、自动备份、PWA 这类改动光看代码看不出对错，这次用 `npm test`（17 个单测）+ Playwright 走查 + `document.fonts.check` 断言兜底；踩坑 28 的教训是：批量改完先 `grep` 复核再谈交付。
>
> 每日诗词补充（2026-09-11）：**"第三方接口不只有活着/死了两种状态，还有'活着但变形了'——内容还是 HTML 首页。"**——候选 API 名单只能当作起点，选型前用 `curl -D -` 实测响应头和响应体；同一轮里我还在编辑 `App.jsx` 时把同一个 effect 整块复制了两份，靠 `grep` 复核才揪掉——多文件改动落盘复核（踩坑 28/39）在每次新功能上线时都要重做一遍。
