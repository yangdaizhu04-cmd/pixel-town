# 待优化清单

基于代码审查，以下是项目可优化的方面，按优先级排序。v0.5.0 完成低风险批次；**v0.6.0（2026-09-11）完成全面审查后的结构性批次**，各项标注见下。

---

## v0.6.0 批次（2026-09-11 · 全面审查执行）

本轮来自一次全量审查（架构/代码质量/性能/依赖/安全），全部落地并验证（lint 0 · 测试 107/107 · build 通过 · 浏览器 CSP 冒烟通过）：

**P0 功能修复**
1. `--sky3` 亮色主题缺失 → 白天（day 时段）整条 body 渐变背景失效塌白底；已在亮色 `:root` 补齐（global.css）。
2. AgentChat 请求无超时 → 网络挂起时聊天页永久卡「输入中」；已加 AbortController 60s 兜底（AgentChat.jsx）。
3. 切页重挂丢失防重记录 → 可对同一待办重复刷 XP；奖励记录提升为模块级「day:id」集合（Todos.jsx）。

**性能**
4. 拆分 State/Dispatch 双 Context（store.jsx），新增 `useDispatch`；dispatch 恒定，只发动作的组件不再被 state 牵连重渲染。
5. Dashboard/Todos/Habits 派生数据 useMemo 化（todos/习惯/结余只随对应切片重算）。
6. Museum 周报原先父组件与子组件各聚合一遍两周数据 → 父组件 useMemo 算一次传下去。
7. Review 每周小结与 Museum 周报共用 weekly.js 的 `aggregate`（聚合口径单一真相）。
8. Ledger 预算 / Weight 目标输入接入共享 `LazyInput`（ui.jsx）：停顿 300ms/回车/失焦才提交，杜绝「每敲一键全树重渲染 + 全量序列化」；Weight 无效中间值不再误清目标。
9. 番茄钟 250ms tick 加签名去重（pomo.js `tell`）：秒数没变不广播，PomoBadge/学习页/标签页标题的无效渲染降约 75%。
10. ledger（4000）/ reviews（500）/ weights（2000）写入封顶 + hydrate 一次性裁剪，封顶 localStorage 序列化成本。
11. App.jsx（461 行）拆出 `lib/hooks.js`（useDayPeriod/usePWA/useAutoBackup/useAchievements/useWeeklyChallenge/useBackupNudge/usePageTitle）与 `lib/nav.js`；成就检测加「全部解锁即早退」，自动备份 effect 收窄依赖，备份提醒修复 stale closure。

**去重与质量**
12. Museum.jsx（444 行）拆为 `components/museum/`（Heatmap/PomoWall/DexGrid/TrophyWall/CustomAchModal/ShareCard + heat.js）；ShareCard effect 依赖收窄为画布实际字段，PomoWall 分组 memo 化。
13. ai.js：查询口径抽到 `lib/queries.js`（dataSummary 与 localAgent 共用），localAgent 改「动作分支 + 规则表」结构（23 条既有单测全过，行为零变化）。
14. 商店「双份真相」清理：ShopModal 花盆价格改用 shop.js `POT_GOODS`，Dashboard 采集种子改用 `HARVEST_COINS`；浇水「选最缺水盆」统一为 shop.js `growersOf/thirstiestOf`（reducer 与 Dashboard 预检共用）。
15. News 页 fetch+dispatch+toast 两份重复合一；ConfirmHost 并发第二个确认框不再吞掉前一个 Promise；English 洗牌改 Fisher-Yates；聊天消息稳定 id（seed/CHAT_ADD/hydrate 回填）替代 index key；charts/Study 杂项 key 修正；裸 `.then` 补防御性 catch。
16. sw.js：两处 `caches.put` 包进 `e.waitUntil`（防响应结束 SW 被杀丢缓存）；缓存版本随 v0.6.0 同步。

**安全**
17. index.html 增加 CSP（兼容单文件内联 JS/CSS/字体与用户自配 API 端点；浏览器实测无违规）。
18. SettingsModal：WebDAV 填 `http://` 明文地址时提示密码未加密传输；API Key 仅存本机的说明原已有之。
19. 依赖检查：官方源 `npm audit` 0 漏洞；react/vite 等 6 个包落后一个小版本（未动，等下次例行升级）。

**未做（评估后明确不做/待观察）**
- localStorage 分片存储、大列表虚拟化：封顶裁剪后当前量级无必要，出现实际卡顿再议。
- 页面级 React.memo 大面积铺开：双 Context 已消除 dispatch 牵连，收益边际递减。

---


## 1. 测试覆盖率不足 ⭐高优先级 ✅主体已完成（2026-09-11 · v0.5.0）

**现状**：7 个测试文件 **90 用例**覆盖 `store`（22）、`pomo`（5）、`weekly`（7）、`poem`（8）、`dates`（11）、`gamify`（14）、`ai`（23）；`@vitest/coverage-v8` 已接入（`npm run test:cov`），`lib/` Statements ~42%。仍零测试：
- `ai.js` 的 `askAI`（SSE 流式，需 mock 流式 fetch）
- `achievements.js`、`challenges.js`（现被 weekly.test 间接触及，无独立用例）
- `news.js`、`weather.js`（在线抓取，需 mock global.fetch）
- `sprites.jsx`（523 行）、`charts.jsx`、`tts.js`、`anim.js`
- 页面组件、UI 组件（mount 不崩溃的 smoke test）

**改进方向**：
- 为 `achievements.js`、`challenges.js` 补独立用例（纯函数，成本低）
- 为 `news.js` / `weather.js` 用 mock fetch 补缓存命中/过期/兜底分支测试
- 页面级先做 smoke test（mount 不崩溃）

**预期收益**：防止 AI 意图解析逻辑回归，提升代码可靠性。

---

## 2. Museum.jsx 过大且存在性能问题 ⭐高优先级 ✅已完成（2026-09-11 · v0.6.0）

拆为 `components/museum/` 六个文件（Heatmap/PomoWall/DexGrid/TrophyWall/CustomAchModal/ShareCard），`ShareCard` effect 依赖收窄为画布实际字段，`PomoWall` 分组 useMemo 化，周报聚合去重（见 v0.6.0 批次第 6/12 条）。

---

## 3. `localAgent` 规则膨胀风险 ⭐中优先级 ✅已完成（2026-09-11 · v0.6.0）

查询口径抽到 `lib/queries.js`（`dataSummary` 与 `localAgent` 共用），`localAgent` 改为「动作分支 + `[{ re, reply }]` 规则表」结构，23 条 ai 单测全过、行为零变化（见 v0.6.0 批次第 13 条）。

---

## 4. App.jsx 主组件副作用过多 ⭐中优先级 ✅已完成（2026-09-11 · v0.6.0）

461 行拆到约 280 行：`lib/hooks.js`（useDayPeriod/usePWA/useAutoBackup/useAchievements/useWeeklyChallenge/useBackupNudge/usePageTitle/useNotifier）+ `lib/nav.js`（页面注册表）；App.jsx 只留布局、切页与页面级动效（见 v0.6.0 批次第 11 条）。

---

## 5. Study.jsx 缺少 memoization ✅已完成（2026-09-11 · v0.5.0）

**现状**：`week`、`days14`、`slots`、`withHour` 已用 `useMemo` 包裹（依赖 `state.study`），`SLOTS` 提为模块常量；每次渲染不再重复计算派生数据。

**预期收益**：减少学习记录增长后的渲染开销。

---

## 6. `weekSpan` 函数重复 ✅已完成（2026-09-11 · v0.5.0）

**现状**：`weekSpan` 已提取到 `dates.js`，`challenges.js` 和 `weekly.js` 改为 import 共用（抽取时漏留 `challenges.js` 的 `weekKey`，测试第一个拦下，见踩坑 48）。

**预期收益**：消除重复，未来修改周计算逻辑只改一处。

---

## 7. localStorage 缓存逻辑重复 ✅已完成（2026-09-11 · v0.5.0）

**现状**：新增 `src/lib/cache.js`（`cacheRead/cacheWrite`，存储 `{at, data}`），`news.js`、`weather.js`、`poem.js` 三处改为复用；weather 顺带改为每个子 key（geo / city:xx）独立条目，查不同城市不再互相挤掉缓存。

**预期收益**：减少约 60 行重复代码，统一缓存策略。

---

## 8. `.oxlintrc.json` 使用非标准注释 ✅已完成（2026-09-11 · v0.5.0）

**现状**：已改名为 `.oxlintrc.jsonc`，oxlint 原生支持 JSONC 注释，规则配置与注释保留，lint 仍按 101 条规则检查。

**预期收益**：避免严格 JSON parser 的兼容问题。

---

## 9. 交接文档事实错误 ✅本项已修正

**现状**：
- 交接文档声称项目「非 git 仓库」→ **2026-09-11 已改**为指出 `.git` 存在（origin: `git@github.com:yangdaizhu04-cmd/pixel-town.git`），`upload.sh` / 一键上传.bat 依赖它
- README 中构建产物大小表述过时 → **已同步**为「约 0.63MB / gzip 约 0.25MB」（`vite build` 实测 dist/index.html 631.85 kB / gzip 251.40 kB，2026-09-11）

**改进方向**：后续改动继续核对这两处说法是否又过期。

**预期收益**：避免交接时误导。

---

## 10. `webdav.js` 使用已废弃的 `unescape` ✅已完成（2026-09-11 · v0.5.0）

**现状**：`authHeader` 已改为 `TextEncoder` + `btoa` 的 UTF-8 安全编码（`utf8ToBase64`），不再用 `unescape(encodeURIComponent(...))`。

**预期收益**：消除废弃 API 依赖。

---

## 总结

项目整体质量良好。v0.5.0 完成低风险批次（测试补强 42→90、`test:cov`、缓存工具、weekSpan 去重、Study memo、`.oxlintrc.jsonc`、unescape 替换）；**v0.6.0 完成全面审查批次**（见文件开头）：P0 三修（sky3 背景失效、聊天卡死、刷 XP）+ 双 Context 拆分 + App/Museum 拆分 + ai.js 规则表化 + 数据封顶 + CSP。当前安全网：**107 用例**（含 17 个页面/组件 smoke）+ lint 0 + 浏览器冒烟。后续关注：官方源 `npm audit` 例行跑、依赖小版本升级、页面级 memo 按需加。