# 待优化清单

基于代码审查，以下是项目可优化的具体方面，按优先级排序。

---

## 1. 测试覆盖率不足 ⭐高优先级 ✅主体已完成（2026-09-11 · v0.5.0）

**现状**：7 个测试文件 **90 用例**覆盖 `store`（22）、`pomo`（5）、`weekly`（7）、`poem`（8）、`dates`（11）、`gamify`（14）、`ai`（23）；`@vitest/coverage-v8` 已接入（`npm run test:cov`），`lib/` Statements ~42%。仍零测试：
- `ai.js` 的 `askAI`（SSE 流式，需 mock 流式 fetch）
- `achievements.js`、`challenges.js`（现被 weekly.test 间接触及，无独立用例）
- `news.js`、`weather.js`（在线抓取，需 mock global.fetch）
- `sprites.jsx`（523 行）、`charts.jsx`、`tts.js`、`anim.js`
- 页面组件、UI 组件（mount 不崩溃的 smoke test）

**改进方向**：
- 为 `achievements.js`、`challenges.js` 补独立用例（纯函数，成本低）
- 为 `news.js` / `weather.js` 用 mock fetch 补缓存命中/过期/兜底分支测试
- 页面级先做 smoke test（mount 不崩溃）

**预期收益**：防止 AI 意图解析逻辑回归，提升代码可靠性。

---

## 2. Museum.jsx 过大且存在性能问题 ⭐高优先级

**现状**：444 行，全项目最大文件，内含 7 个子组件未拆分。关键问题：
```jsx
// ShareCard 的 useEffect 依赖整个 state 对象
useEffect(() => { /* ...重绘 canvas */ }, [state]) // ← state 每次变化都重绘
```

**改进方向**：
- 拆分 `Heatmap`、`PomoWall`、`DexGrid`、`AchGrid`、`ShareCard` 为独立文件
- `ShareCard` 的 effect 依赖改为精确值（如 canvas 所需字段的 hash）
- 使用 `useMemo` 缓存 canvas 绘制

**预期收益**：减少不必要的重绘，提升可读性和可维护性。

---

## 3. `localAgent` 规则膨胀风险 ⭐中优先级

**现状**：`ai.js:localAgent` 是不断增长的 if-else 链（~14 个意图分支），与 `dataSummary` 存在数据聚合逻辑重复（习惯统计、余额计算等）。✅ 已于 2026-09-11 为 `dataSummary` / `parseActs` / `localAgent` 全分支补上 23 条单测（ai.test.js）。

**改进方向**：
- 将查询类逻辑抽成 `lib/queries.js` 纯函数，`dataSummary` 和 `localAgent` 共用
- 规则表化（用 `[{ patterns, handler }]` 结构代替 if-else 链）

**预期收益**：防止单点逻辑膨胀，便于维护和测试。

---

## 4. App.jsx 主组件副作用过多 ⭐中优先级

**现状**：443 行，承载 14 个 `useEffect`（番钟、备份、安装引导、成就检测等）。

**改进方向**：
- 将成就检测、每周挑战、自动备份等提取为自定义 hook（如 `useAchievements(state, dispatch)`）
- 安装引导逻辑提取为 `usePWAManagement()` hook
- App.jsx 只保留布局和路由

**预期收益**：减少认知负担，每个 hook 可独立测试。

---

## 5. Study.jsx 缺少 memoization ✅已完成（2026-09-11 · v0.5.0）

**现状**：`week`、`days14`、`slots`、`withHour` 已用 `useMemo` 包裹（依赖 `state.study`），`SLOTS` 提为模块常量；每次渲染不再重复计算派生数据。

**预期收益**：减少学习记录增长后的渲染开销。

---

## 6. `weekSpan` 函数重复 ✅已完成（2026-09-11 · v0.5.0）

**现状**：`weekSpan` 已提取到 `dates.js`，`challenges.js` 和 `weekly.js` 改为 import 共用（抽取时漏留 `challenges.js` 的 `weekKey`，测试第一个拦下，见踩坑 48）。

**预期收益**：消除重复，未来修改周计算逻辑只改一处。

---

## 7. localStorage 缓存逻辑重复 ✅已完成（2026-09-11 · v0.5.0）

**现状**：新增 `src/lib/cache.js`（`cacheRead/cacheWrite`，存储 `{at, data}`），`news.js`、`weather.js`、`poem.js` 三处改为复用；weather 顺带改为每个子 key（geo / city:xx）独立条目，查不同城市不再互相挤掉缓存。

**预期收益**：减少约 60 行重复代码，统一缓存策略。

---

## 8. `.oxlintrc.json` 使用非标准注释 ✅已完成（2026-09-11 · v0.5.0）

**现状**：已改名为 `.oxlintrc.jsonc`，oxlint 原生支持 JSONC 注释，规则配置与注释保留，lint 仍按 101 条规则检查。

**预期收益**：避免严格 JSON parser 的兼容问题。

---

## 9. 交接文档事实错误 ✅本项已修正

**现状**：
- 交接文档声称项目「非 git 仓库」→ **2026-09-11 已改**为指出 `.git` 存在（origin: `git@github.com:yangdaizhu04-cmd/pixel-town.git`），`upload.sh` / 一键上传.bat 依赖它
- README 中构建产物大小表述过时 → **已同步**为「约 0.63MB / gzip 约 0.25MB」（`vite build` 实测 dist/index.html 631.85 kB / gzip 251.40 kB，2026-09-11）

**改进方向**：后续改动继续核对这两处说法是否又过期。

**预期收益**：避免交接时误导。

---

## 10. `webdav.js` 使用已废弃的 `unescape` ✅已完成（2026-09-11 · v0.5.0）

**现状**：`authHeader` 已改为 `TextEncoder` + `btoa` 的 UTF-8 安全编码（`utf8ToBase64`），不再用 `unescape(encodeURIComponent(...))`。

**预期收益**：消除废弃 API 依赖。

---

## 总结

项目整体质量良好。v0.5.0 已把低风险项清零（测试补强 42→90、`test:cov` 覆盖率脚本、缓存工具、weekSpan 去重、Study memo、`.oxlintrc.jsonc`、unescape 替换）；当前最需要关注的是**大文件拆分**（Museum.jsx）与 **ai.js 规则表化 / App.jsx 抽 hooks** 等高风险重构，且已有 90 用例安全网可托底。