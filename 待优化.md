# 待优化清单

基于代码审查，以下是项目可优化的具体方面，按优先级排序。

---

## 1. 测试覆盖率不足 ⭐高优先级

**现状**：仅 4 个测试文件覆盖 `store`、`pomo`、`weekly`、`poem`（42 用例）。核心模块零测试：
- `ai.js`（AI 聊天 + 离线 agent，220 行）
- `achievements.js`、`challenges.js`（`challenges.js` 部分函数已被 weekly.test 触及，无独立用例）
- `news.js`、`weather.js`
- `sprites.jsx`（523 行）
- 所有页面组件、UI 组件

**改进方向**：
- 优先为 `ai.js`（`localAgent` 离线分支 + `parseActs`）补充单元测试
- 为 `dates.js`、`gamify.js` 纯函数补测试
- 引入 `@vitest/coverage-v8` 监控覆盖率，目标先覆盖 `lib/` 全部纯函数
- 页面级先做 smoke test（mount 不崩溃）

**预期收益**：防止 AI 意图解析逻辑回归，提升代码可靠性。

---

## 2. Museum.jsx 过大且存在性能问题 ⭐高优先级

**现状**：444 行，全项目最大文件，内含 7 个子组件未拆分。关键问题：
```jsx
// ShareCard 的 useEffect 依赖整个 state 对象
useEffect(() => { /* ...重绘 canvas */ }, [state]) // ← state 每次变化都重绘
```

**改进方向**：
- 拆分 `Heatmap`、`PomoWall`、`DexGrid`、`AchGrid`、`ShareCard` 为独立文件
- `ShareCard` 的 effect 依赖改为精确值（如 canvas 所需字段的 hash）
- 使用 `useMemo` 缓存 canvas 绘制

**预期收益**：减少不必要的重绘，提升可读性和可维护性。

---

## 3. `localAgent` 规则膨胀风险 ⭐中优先级

**现状**：`ai.js:localAgent` 是不断增长的 if-else 链（~14 个意图分支），与 `dataSummary` 存在数据聚合逻辑重复（习惯统计、余额计算等）。

**改进方向**：
- 将查询类逻辑抽成 `lib/queries.js` 纯函数，`dataSummary` 和 `localAgent` 共用
- 规则表化（用 `[{ patterns, handler }]` 结构代替 if-else 链）
- 为 `localAgent` 补单元测试

**预期收益**：防止单点逻辑膨胀，便于维护和测试。

---

## 4. App.jsx 主组件副作用过多 ⭐中优先级

**现状**：443 行，承载 14 个 `useEffect`（番钟、备份、安装引导、成就检测等）。

**改进方向**：
- 将成就检测、每周挑战、自动备份等提取为自定义 hook（如 `useAchievements(state, dispatch)`）
- 安装引导逻辑提取为 `usePWAManagement()` hook
- App.jsx 只保留布局和路由

**预期收益**：减少认知负担，每个 hook 可独立测试。

---

## 5. Study.jsx 缺少 memoization ⭐中优先级

**现状**：`week`、`days14`、`slots`、`slotMin` 等派生数据每次渲染都重新计算，无 `useMemo`。

**改进方向**：对依赖 `state.study` 的派生计算用 `useMemo` 包裹。

**预期收益**：减少学习记录增长后的渲染开销。

---

## 6. `weekSpan` 函数重复 ⭐中优先级

**现状**：`challenges.js` 和 `weekly.js` 有完全相同的 `weekSpan` 函数（~6 行）。

**改进方向**：提取到 `dates.js`，两处改为 import。

**预期收益**：消除重复，未来修改周计算逻辑只改一处。

---

## 7. localStorage 缓存逻辑重复 ⭐低优先级

**现状**：`news.js`、`weather.js`、`poem.js` 各自实现了相同的 localStorage + TTL 缓存模式（poem 是第三处复制）。

**改进方向**：提取通用缓存工具函数（如 `cachedFetch(key, ttl, fetcher)`）。

**预期收益**：减少 ~60 行重复代码，统一缓存策略。

---

## 8. `.oxlintrc.json` 使用非标准注释 ⭐低优先级

**现状**：JSON 文件中使用了 `//` 注释（JSONC 语法），严格 parser 会报错。

**改进方向**：改用 `.oxlintrc.jsonc` 后缀或移除注释。

**预期收益**：避免工具链兼容问题。

---

## 9. 交接文档事实错误 ✅本项已修正

**现状**：
- 交接文档声称项目「非 git 仓库」→ **2026-09-11 已改**为指出 `.git` 存在（origin: `git@github.com:yangdaizhu04-cmd/pixel-town.git`），`upload.sh` / 一键上传.bat 依赖它
- README 中构建产物大小表述过时 → **已同步**为「约 0.63MB / gzip 约 0.25MB」（`vite build` 实测 dist/index.html 631.85 kB / gzip 251.40 kB，2026-09-11）

**改进方向**：后续改动继续核对这两处说法是否又过期。

**预期收益**：避免交接时误导。

---

## 10. `webdav.js` 使用已废弃的 `unescape` ⭐低优先级

**现状**：`authHeader` 使用 `unescape(encodeURIComponent(...))` 做 Base64 编码。

**改进方向**：使用 `TextEncoder` + `btoa` 的现代替代方案。

**预期收益**：消除废弃 API 依赖。

---

## 总结

项目整体质量良好，最需要关注的是**测试覆盖**和**大文件拆分**两项。其他优化点优先级较低，可逐步改进。